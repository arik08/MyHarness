# OpenHarness 패치 노트

이 문서는 로컬 Git 커밋 기록을 바탕으로, 비개발자도 이해하기 쉽게 날짜별 주요 개선사항만 정리한 것입니다.
테스트 보강, 문서의 작은 문구 수정, 내부 정리 같은 자잘한 변경은 관련 기능 아래에 묶었습니다. 단, 직접 추가한 스킬은 사용자가 할 수 있는 일이 늘어나는 큰 변화라 별도로 드러내고, 자동 생성되는 `learned-*` 스킬은 주요 기능 목록에서 제외했습니다.

표기 기준:

- `원본 OpenHarness`: upstream OpenHarness 또는 다른 contributor의 커밋입니다.
- `Myeongcheol`: Git author가 `Myeongcheol` 또는 `Myeongcheol_Oh`인 커밋입니다.

## 2026-04-01 (원본 OpenHarness)

- OpenHarness 초기 버전 기반이 만들어졌습니다.

## 2026-04-02 (원본 OpenHarness)

- README와 Windows 실행 호환성 같은 기본 문서/환경 정리가 있었습니다.

## 2026-04-03 (원본 OpenHarness)

- 여러 에이전트가 역할을 나눠 일하기 위한 기본 구조와 CI 기반이 추가되었습니다.

## 2026-04-04 (원본 OpenHarness)

- OpenAI 호환 API, 대화 압축, 이어받기 같은 기본 실행 기능이 보강되었습니다.

## 2026-04-05 (원본 OpenHarness)

- 인증, 제공자 연결, 설치/배포, TUI 표시 안정화가 전반적으로 정리되었습니다.

## 2026-04-06 (원본 OpenHarness)

- Ohmo, 인증 프로필, Kimi 지원, Windows 실행 안정화 같은 upstream 기능 보강이 있었습니다.

## 2026-04-07 (원본 OpenHarness)

- 인증 오류 안내, 웹 가져오기, Windows 콘솔, 터미널 입력 관련 안정화가 있었습니다.

## 2026-04-08 (원본 OpenHarness)

- 권한 모드, MCP 연결, 서브프로세스 조회, Ohmo 첨부파일 처리 등이 보강되었습니다.

## 2026-04-09 (원본 OpenHarness)

- TUI 표시, Bash timeout, 경로/네트워크 보호 같은 사용 안정성이 개선되었습니다.

## 2026-04-10 (원본 OpenHarness)

- 에이전트 실행, 설치 복구, 스킬 파싱, 대화 압축/복원 안정화가 있었습니다.

## 2026-04-11 (원본 OpenHarness)

- Gemini 제공자 추가와 TUI 표시/도구 결과 표시 개선이 있었습니다.

## 2026-04-12 (원본 OpenHarness)

- Claude 인증, Docker 샌드박스, 압축/기본 URL 관련 안정화가 있었습니다.

## 2026-04-13 (원본 OpenHarness)

- 파일 저장 안전성, Bash 출력, TUI 갱신, Windows PowerShell 설치 지원이 보강되었습니다.

## 2026-04-14 (원본 OpenHarness)

- Todo, 병렬 도구 실행, 세션 복원, 스트리밍 표시 관련 안정화가 있었습니다.

## 2026-04-15 (원본 OpenHarness)

- 설치 감지, 원격 채널 허용 목록, 프로필별 기본 URL 설정이 정리되었습니다.

## 2026-04-16 (원본 OpenHarness)

- MiniMax 제공자 추가, 에이전트 옵션 전달, Autopilot 대시보드 개선이 있었습니다.

## 2026-04-17 (원본 OpenHarness)

- 모델 선택, 공유 채팅 분리, 플러그인 안전성, 터미널 입력 안정화가 있었습니다.

## 2026-04-18 (원본 OpenHarness)

- v0.1.7 릴리스가 반영되었습니다.

## 2026-04-19 (원본 OpenHarness)

- Windows 터미널 입력과 비동기 작업 대기 관련 문제가 고쳐졌습니다.

## 2026-04-20 (원본 OpenHarness)

- 세션 복원, 스트리밍 출력, 작업자 시스템 프롬프트 전달이 안정화되었습니다.

## 2026-04-21 (원본 OpenHarness)

- 쉘 서브프로세스가 입력 대기 상태로 멈추는 문제를 줄였습니다.

## 2026-04-22 (원본 OpenHarness)

- dry-run 안전 미리보기와 Backspace 입력 관련 수정이 들어갔습니다.

## 2026-04-23 (원본 OpenHarness)

- Autopilot 실행 안전성, 외부 채널 헬퍼, 슬래시 명령 자동완성이 정리되었습니다.

## 2026-04-24 (원본 OpenHarness)

- 대화 압축과 Windows 터미널 입력 관련 안정화가 있었습니다.

## 2026-04-25 (원본 OpenHarness)

- Telegram 설정과 플러그인 도구 자동 등록/지연 로딩이 보강되었습니다.

## 2026-04-25 (Myeongcheol)

이 날의 가장 큰 변화는 OpenHarness가 처음으로 웹 앱의 형태를 갖추기 시작했다는 점입니다. 기존에는 명령줄과 터미널 UI에서 쓰는 도구에 가까웠기 때문에, 대화 기록을 다시 찾거나 생성된 파일을 확인하거나 설정을 바꾸는 일이 화면 안에서 매끄럽게 이어지지는 않았습니다. 그래서 브라우저에서 OpenHarness를 사용할 수 있는 첫 웹 UI를 만들었습니다.

- 웹 화면에서 메시지를 입력하면 실제 OpenHarness 작업으로 이어지고, 응답이 다시 브라우저에 표시되도록 연결했습니다. 단순한 화면 시안이 아니라, 실제 에이전트 실행과 이어지는 첫 구조였습니다.
- 처음 웹 UI를 만들 때부터 단순히 기능을 붙이는 데서 그치지 않고, CLI 도구를 웹 앱으로 옮겼을 때 어색하지 않도록 레이아웃과 시각적 밀도를 함께 잡기 시작했습니다. 대화 영역, 사이드바, 입력창, 보조 패널이 한 화면 안에서 서로 밀어내지 않고 공존하는 것이 중요한 기준이었습니다.
- 대화 기록을 웹에서 보고 다시 선택할 수 있는 기반을 넣었습니다. 터미널 중심 사용에서는 지난 작업을 되찾는 일이 불편했기 때문에, 웹 UI에서는 히스토리를 중요한 첫 기능으로 잡았습니다.
- 에이전트가 만든 파일이나 이미지, 문서 결과물을 따로 확인할 수 있도록 아티팩트 영역의 기초를 만들었습니다. 이후 결과물을 브라우저에서 바로 열어보는 기능들이 여기에 이어졌습니다.
- 프로젝트 안의 파일을 경로로 직접 입력하는 대신, 웹 화면에서 고를 수 있는 흐름을 추가했습니다. 웹 UI를 만든 목적이 “덜 외우고, 더 보고 고르는 방식”이었기 때문에 필요한 작업이었습니다.
- Windows에서 웹 UI를 쉽게 켤 수 있도록 실행 파일을 추가했습니다. 개발자용 명령을 매번 조합하지 않고도 로컬 웹 화면을 열 수 있게 하려는 목적이었습니다.
- 웹 세션을 오래 켜 두고 쓰는 상황을 고려해, 세션이 끝날 때 남는 연결과 상태를 정리하는 부분도 함께 보강했습니다.
- 모델을 고를 때 최신 OpenAI 계열 기본값과 추론 강도 옵션을 쓸 수 있도록 정리했습니다. 웹 UI에서 설정을 다루기 시작했기 때문에, 모델 선택 경험도 같이 맞춰야 했습니다.
- 스킬 사용 이력이 사이드바와 채팅에서 더 자연스럽게 보이도록 고쳤습니다. 사용자가 “지금 어떤 보조 기능이 쓰였는지”를 화면에서 이해할 수 있게 하려는 변경입니다.
- 프로젝트 삭제는 실수하면 피해가 크기 때문에, 바로 삭제하지 않고 한 번 더 확인하는 흐름으로 바꿨습니다.
- 브라우저 점검 중 생기는 임시 산출물이 Git 변경 목록에 섞이지 않도록 Playwright MCP 관련 ignore 규칙도 정리했습니다.

## 2026-04-26 (Myeongcheol)

전날 만든 웹 UI를 “켜지는 화면”에서 “실제로 오래 작업할 수 있는 화면”으로 끌어올리는 데 집중한 날입니다. 대화, 히스토리, 아티팩트, 설정, 세션 관리가 한 화면 안에서 계속 이어져야 했기 때문에 사용 흐름과 안정성을 같이 손봤습니다.

- 도움말과 Plan Mode 표시를 정리했습니다. 사용자가 지금 명령을 실행하는 중인지, 먼저 계획을 세우는 중인지 헷갈리지 않게 하고, 화면 안에서 할 수 있는 일을 더 쉽게 파악하게 하려는 목적이었습니다.
- 화면 밀도와 테마를 다듬었습니다. 기능이 빠르게 늘어나면서 화면이 답답해질 수 있었기 때문에, 버튼과 패널 간격, 선택지 모양, 텍스트 크기를 더 오래 보기 편한 쪽으로 조정했습니다.
- 디자인적으로는 “개발 도구지만 너무 터미널스럽지 않고, 그렇다고 과한 대시보드처럼 보이지 않는” 균형을 잡는 데 시간을 썼습니다. 패널을 큼직한 카드처럼 띄우기보다 정보가 조밀하게 정리되도록 하고, 버튼과 라벨은 작지만 눌러야 할 위치가 분명하게 보이도록 맞췄습니다.
- 사이드바, 대화 영역, 보조 패널이 동시에 보이는 상황에서도 시선이 흩어지지 않게 레이아웃을 조정했습니다. 특히 히스토리와 아티팩트처럼 자주 열고 닫는 영역은 화면을 크게 흔들지 않도록 다듬었습니다.
- 여러 세션을 한꺼번에 열어도 화면과 백엔드가 감당할 수 있도록 동시 세션 수를 제한했습니다. 무작정 많이 열리는 것보다, 사용자가 상태를 이해할 수 있는 범위 안에서 관리되게 한 것입니다.
- 위험한 명령에 대한 안내를 더 분명하게 바꿨습니다. 웹에서는 클릭 한 번으로 작업이 이어질 수 있으므로, 삭제나 덮어쓰기 같은 작업은 사용자가 위험을 인지한 뒤 진행하도록 했습니다.
- 여러 대화 흐름을 나눠 볼 수 있는 세 방향 채팅 슬롯을 추가했습니다. 하나의 대화에 모든 실험과 참고 내용을 섞지 않고, 작업 흐름을 분리해 볼 수 있게 하려는 기능입니다.
- 파일 변경이 동시에 겹칠 때 생길 수 있는 충돌을 줄이기 위해, 변경 작업이 진행 중이면 다른 변경이 기다리도록 하는 안전장치를 보강했습니다.
- 히스토리 패널을 더 자연스럽게 다듬었습니다. 과거 대화를 불러오고, 선택하고, 삭제하는 동작이 현재 작업 중인 채팅을 불필요하게 흔들지 않도록 고쳤습니다.
- 아티팩트 패널을 본격적으로 사용 가능한 형태로 정리했습니다. 생성 결과물을 열어보는 것뿐 아니라 원본 내용까지 확인할 수 있게 해서, 결과물이 어떻게 만들어졌는지 추적하기 쉬워졌습니다.
- 답변 아래의 액션 버튼과 스트리밍 상태 표시를 정리했습니다. 답변이 생성 중인지 완료됐는지, 다음에 무엇을 할 수 있는지 더 분명히 보이게 하려는 작업이었습니다.
- 웹 자산 패키징을 정리했습니다. 초기 작업 중 설치 산출물이 과하게 섞였기 때문에, 실제 실행과 배포에 필요한 파일 중심으로 남기고 불필요한 파일은 걷어냈습니다.
- Windows 환경에서 웹 UI를 쓰는 경우를 중요하게 보고, 경로 처리, 실행 파일, 콘솔 동작, 테스트를 보강했습니다.
- 프로젝트마다 다른 P-GPT 설정을 둘 수 있게 했습니다. 회사 내부용, 개인용, 테스트용 연결을 같은 전역 설정에 억지로 섞지 않기 위한 변경입니다.
- JSON 기반 로그인과 자격 증명 예시를 추가했습니다. 설정값을 매번 손으로 입력하는 대신, 정리된 파일을 보고 맞춰 넣을 수 있게 하려는 목적이었습니다.
- 웹 설정에서 고른 값이 실제 작업 세션까지 전달되도록 보강했습니다. 화면에서 설정한 내용과 에이전트가 실제로 쓰는 환경이 어긋나지 않게 하는 데 초점을 맞췄습니다.
- 채팅 히스토리의 이름과 오류 표시를 다듬었습니다. 대화가 많아질수록 “어떤 대화였는지”와 “무엇이 실패했는지”를 바로 이해하는 것이 중요해졌기 때문입니다.
- workspace 기준으로 살아 있는 세션을 구분할 수 있게 했습니다. 프로젝트별로 작업 맥락을 나눠 관리하기 위한 기반입니다.
- 설정 화면에서 시스템 프롬프트, 다운로드 동작, P-GPT 자격 증명을 다룰 수 있게 확장했습니다. 사용자가 설정 파일을 직접 열어 고치는 일을 줄이려는 방향입니다.
- 설치와 런타임 준비 과정을 보강했습니다. 로컬 설치 후 웹 UI를 바로 켰을 때 빠진 준비 작업 때문에 실패하는 일을 줄이기 위한 작업입니다.
- 프로젝트 파일 목록과 라벨을 정리했습니다. 너무 넓은 범위의 파일이 노출되거나 이름이 헷갈리면 선택 실수가 생기기 쉬워서, 더 안전하고 알아보기 쉬운 표시로 바꿨습니다.
- 접근이 막히는 웹사이트를 조사할 때 일반 검색이나 단순 가져오기로 끝나지 않도록 `insane-search` 스킬을 추가했습니다. 403/차단 페이지, X/Twitter, Reddit, YouTube, Naver, GitHub, Medium 같은 사이트에서 여러 우회 경로와 공개 API, 실제 브라우저 렌더링까지 시도할 수 있는 조사용 능력을 넣은 것입니다.
- 디자인 결과물을 더 잘 만들기 위해 `huashu-design` 스킬 묶음을 도입하고 영어 설명도 정리했습니다. 웹 UI가 단순 채팅 화면을 넘어, 프로토타입, 시각 자료, 슬라이드형 결과물까지 만들 수 있는 방향으로 확장될 수 있게 하려는 시도였습니다.
- 자동으로 생기는 `learned-*` 스킬들도 일부 기록되었지만, 이는 사용 중 쌓이는 보조 학습 자료 성격이라 주요 기능 추가로 보지는 않았습니다.

## 2026-04-28 (Myeongcheol)

웹 UI가 기능 면에서는 어느 정도 갖춰진 뒤라, 이날은 “한 번에 만든 기능들을 실제 제품처럼 다듬는 작업”에 가까웠습니다. 히스토리, 메시지, 아티팩트, 설정, 모달이 각자 따로 노는 느낌을 줄이고, 사용자가 하나의 앱을 쓰는 것처럼 느끼게 하는 데 집중했습니다.

- 히스토리 새로고침 버튼과 주변 배치를 실제 화면을 보며 조정했습니다. 대화 목록을 갱신하는 동작이 눈에 잘 들어오고, 버튼이 주변 UI와 어울리도록 맞췄습니다.
- 디자인 작업의 비중이 컸습니다. 버튼 하나의 위치, 텍스트가 줄바꿈되는 방식, 패널 안쪽 여백, 작은 상태 표시가 전체 화면의 완성도를 좌우해서, 실제 캡처를 보며 계속 미세 조정했습니다.
- 전체 화면이 한 가지 색감이나 무거운 장식에 끌려가지 않도록 조정했습니다. 작업 도구답게 차분하게 보이되, 메시지와 결과물, 설정 영역이 어디인지 빠르게 구분되도록 대비와 간격을 맞췄습니다.
- 첨부파일 작성 영역을 다듬었습니다. 파일을 붙이고 확인하는 과정이 웹 UI에서 자주 쓰일 수 있기 때문에, 답답하거나 깨져 보이지 않게 레이아웃을 정리했습니다.
- 이미지 미리보기 표시를 고쳤습니다. 이미지가 너무 크게 튀어나오거나 프레임 안에서 어색하게 잘리면 결과 검토가 어려워서, 더 안정적으로 보이게 했습니다.
- 긴 대화에서 토큰 정보가 화면을 복잡하게 만들지 않도록, 접힌 토큰 표시와 인라인 표시 방식을 차분하게 정리했습니다.
- 모델 설정 화면을 집중적으로 손봤습니다. 제공자 선택, 모델 정보, 저장 상태가 사용자가 이해하기 쉬운 순서로 보이고, 화면이 깨지지 않도록 여러 캡처를 보며 조정했습니다.
- 모달과 설정 화면은 특히 밀도를 많이 조정했습니다. 너무 넓고 비어 보이면 설정의 무게감이 떨어지고, 너무 촘촘하면 읽기 어려워져서 입력칸, 설명, 버튼 사이의 간격을 여러 번 맞췄습니다.
- 이미지와 첨부파일처럼 실제 결과물이 보이는 영역은 “검토 가능한 크기”를 우선했습니다. 장식적으로 예쁘게 보이는 것보다, 사용자가 내용을 제대로 확인할 수 있도록 프레임과 미리보기 비율을 조정했습니다.
- P-GPT 인증과 제공자 등록 흐름을 정리했습니다. 로컬 설정과 프로젝트 설정이 함께 쓰이면서 생길 수 있는 혼란을 줄이고, 설정 후 실제 연결을 확인하는 흐름을 더 안정적으로 만들었습니다.
- POSCO GPT 전용으로 따로 흩어져 있던 흐름은 P-GPT 중심으로 정리했습니다. 비슷한 연결 방식이 여러 군데 나뉘어 있으면 유지보수와 사용자 이해가 모두 어려워지기 때문입니다.
- 프로젝트 지침이 웹 UI 작업에도 자연스럽게 반영되도록 정리했습니다. AGENTS.md나 CLAUDE.md 계열 문서를 읽어 에이전트가 작업 맥락을 놓치지 않게 하려는 목적이었습니다.
- 시스템 프롬프트 구성도 함께 정리했습니다. 사용자 설정, 프로젝트 지침, 실행 환경 정보가 중복되거나 빠지지 않게 맞춰, 에이전트 동작이 더 예측 가능해지도록 했습니다.
- 웹에서 파일 편집이나 명령 실행까지 이어지는 만큼, Bash와 파일 관련 도구의 안전 안내를 보강했습니다. 사용자가 의도하지 않은 변경을 더 빨리 알아차리게 하려는 작업입니다.
- 화면 상태와 실제 백엔드 상태가 어긋나는 문제를 줄이기 위해 동기화 흐름을 개선했습니다. 웹 UI에서는 특히 “보이는 상태”와 “실제 작업 상태”가 맞는 것이 중요했습니다.
- Windows에서 웹 UI를 켜고 로그를 확인하는 과정이 더 예측 가능하도록, 배치 파일과 PowerShell 실행 스크립트를 정리했습니다.
- 로컬 설치가 이전 상태 때문에 꼬이지 않도록 설치 프로그램도 함께 손봤습니다.
- Bash 사용, 웹 가져오기 실패, 검색 결과 없음처럼 반복적으로 겪은 상황은 자동 학습 기록으로 남겼습니다. 다만 이것은 사용 중 생기는 보조 기록이라, 직접 추가한 스킬과는 구분했습니다.
- README와 OpenHarness 설명 문서를 현재 웹 UI 흐름에 맞게 업데이트했습니다. 문서가 실제 사용 방법과 어긋나지 않도록 맞춘 작업입니다.
- 히스토리 버튼, 첨부 영역, 이미지 미리보기, 모델 설정 화면은 실제 캡처를 보며 확인했습니다. 말로만 정리한 것이 아니라, 깨지기 쉬운 화면을 직접 보면서 조정했습니다.

## 2026-04-29 (Myeongcheol)

이날은 웹 UI를 계속 다듬으면서, 동시에 작업 과정에서 생기는 임시 산출물을 정리한 날입니다. 화면을 좋아지게 만드는 일과, 그 화면을 확인하면서 생기는 로그와 캡처가 저장소를 어지럽히지 않게 하는 일을 같이 처리했습니다.

- 체크리스트 UI를 다듬었습니다. 항목을 확인하고 접고 펼치는 요소는 자주 보게 되는 부분이라, 버튼과 텍스트가 어색하게 튀지 않도록 정리했습니다.
- 체크리스트 컨트롤의 밀도를 낮췄습니다. 화면이 무겁고 투박해 보이지 않도록 패딩과 간격, 버튼 형태를 조금 더 슬림하게 맞췄습니다.
- 체크리스트는 작은 컴포넌트지만 실제로는 디자인 시간이 많이 들어간 부분입니다. 접힌 상태, 펼친 상태, 항목이 많을 때, 설명이 길 때를 각각 보면서 버튼 크기와 줄 간격, 텍스트 무게를 계속 맞췄습니다.
- “예쁘지만 큰 UI”가 아니라 “작업 중 계속 켜 둬도 방해되지 않는 UI”를 목표로 했습니다. 그래서 둥근 모서리나 여백을 과하게 쓰지 않고, 필요한 정보가 차분하게 정렬되도록 조정했습니다.
- 웹 UI와 터미널 UI가 같은 제품처럼 느껴지도록 색, 라벨, 간격, 컨트롤 표현을 맞췄습니다. 기능은 달라도 전체 인상이 따로 놀지 않게 하려는 작업이었습니다.
- 긴 텍스트나 좁은 화면에서 메시지와 패널이 겹치거나 잘리는 문제를 줄였습니다. 실제 사용 중 가장 거슬리는 종류의 UI 깨짐을 줄이는 데 초점을 뒀습니다.
- 4월 26일에 넣었던 큰 디자인 스킬 묶음은 기능은 넓었지만 데모, 음원, 샘플 이미지까지 함께 들어와 저장소가 무거워질 수 있었습니다. 그래서 실제 OpenHarness 작업 흐름에 맞게 더 가벼운 스킬들로 나누는 방향으로 정리했습니다.
- `ui-design-essence` 스킬을 추가했습니다. 새 화면이나 컴포넌트를 만들 때 단순히 보기 좋게 꾸미는 것이 아니라, 목적, 사용자, 밀도, 시각적 위계를 먼저 정하고 일관된 디자인 기준으로 다듬기 위한 스킬입니다.
- `visual-artifact` 스킬을 추가했습니다. 보고서, 대시보드, 인포그래픽, 비교표, 발표용 HTML 같은 결과물을 브라우저에서 바로 열 수 있는 단일 시각 자료로 만들기 위한 스킬입니다.
- `visual-review` 스킬을 추가했습니다. 만든 HTML 결과물이 실제 브라우저에서 깨지지 않는지, 글자가 잘리거나 넘치지 않는지, PDF나 스크린샷으로 내보내도 괜찮은지 점검하기 위한 스킬입니다.
- `playwright-capture` 스킬을 추가했습니다. HTML 페이지를 실제 Chromium으로 렌더링해 스크린샷이나 PDF로 저장하고, 발표 자료나 보고서에 쓸 수 있는 상태로 캡처하기 위한 스킬입니다.
- 이 스킬들은 웹 UI 디자인을 계속 다듬으면서 필요해진 작업 흐름을 반영한 것입니다. 화면을 만들고, 시각 자료로 정리하고, 실제 브라우저에서 검수하고, 캡처까지 이어지는 과정을 OpenHarness 안에서 처리할 수 있게 하는 것이 목적이었습니다.
- 체크리스트 상태, 입력 경계, 소프트 리저브 표시처럼 깨지기 쉬운 부분은 화면 캡처로 확인하면서 조정했습니다.
- 디자인 스킬 묶음을 가져오는 과정에서 큰 데모 파일과 음원, 샘플 이미지가 과하게 들어올 수 있다는 점을 확인했고, 어떤 파일을 실제 저장소에 남기고 어떤 것은 제외할지 정리 기준을 다시 잡았습니다.
- UI 점검을 반복하면서 생긴 로그와 스크린샷이 프로젝트 루트에 쌓이는 문제가 있었기 때문에, 이런 검사 산출물이 Git 변경 목록에 섞이지 않도록 ignore 규칙을 추가했습니다.
- 브라우저 점검용 임시 프로필 폴더도 Git 추적 대상에서 제외했습니다. 캐시와 내부 로그가 대량으로 생기는 폴더라, 기능 변경과 함께 관리할 필요가 없다고 판단했습니다.
- 이미 Git이 추적하던 UI 점검 로그와 임시 브라우저 파일은 인덱스에서 제거했습니다. 앞으로는 실제 파일이 남아 있어도 커밋 대상에 올라오지 않게 하기 위한 정리입니다.
- 같은 문제가 반복되지 않도록 작업 지침도 보강했습니다. 앞으로 화면 점검 로그와 스크린샷은 루트가 아니라 무시되는 로컬 작업 폴더에 모으도록 기준을 세웠습니다.

## 2026-04-30 (Myeongcheol)

이날은 OpenHarness를 개인 작업용 MyHarness 구조로 본격 정리한 날입니다. 단순히 화면에 보이는 이름만 바꾼 것이 아니라, Python 패키지, 설정 폴더, 실행 스크립트, 웹 서버, 문서, 테스트가 같은 기준을 바라보도록 맞췄습니다. 동시에 웹 UI에서 오래 작업할 때 자주 걸리던 세션 복구, 권한 모드, 프로젝트 파일 관리 문제도 같이 손봤습니다.

- Python 런타임 패키지를 `src/openharness`에서 `src/myharness`로 옮겼습니다. 내부 import, 테스트, 패키징 설정이 새 이름을 기준으로 맞춰져서 코드 구조에서 OpenHarness와 MyHarness가 섞여 보이는 혼란을 줄였습니다.
- PyPI 패키지 이름과 실행 명령도 MyHarness 중심으로 정리했습니다. 새 명령은 `myharness`, `myh`이고, 기존 습관을 고려해 짧은 `oh` 명령은 계속 유지했습니다.
- 전역/프로젝트 설정 폴더를 `.openharness`에서 `.myharness`로 바꿨습니다. 사용자 설정, 세션, 작업 기록, autopilot 상태, memory 파일이 MyHarness 전용 위치에 모이도록 경로 규칙을 정리했습니다.
- 환경변수 접두사도 `OPENHARNESS_*`에서 `MYHARNESS_*`로 바꿨습니다. 작업공간 범위, 셸 선택, 설정/데이터/로그 폴더 같은 런타임 설정이 새 이름과 맞게 동작합니다.
- Windows 실행 파일과 바로가기를 `run_myharness_web.bat`, `MyHarness Web.url` 기준으로 정리했습니다. PowerShell 웹 서버 실행 스크립트도 새 이름으로 바꾸고, 이전 OpenHarness 서버 스크립트는 제거했습니다.
- 설치 프로그램과 개발 설치 스크립트를 새 폴더 구조에 맞게 고쳤습니다. `.myharness-venv`, `.myharness/settings.json`, MyHarness 패키지 경로를 기준으로 로컬 설치와 복구가 이어지도록 하고, 필요한 의존성 확인 목록도 최신화했습니다.
- README, SHOWCASE, AGENTS 작업 지침, issue template, Autopilot 워크플로/스냅샷 문구를 MyHarness 기준으로 업데이트했습니다. 사용자가 따라 하는 문서와 실제 실행 파일 이름이 어긋나지 않게 맞춘 작업입니다.
- 웹 UI의 localStorage 키와 iframe 메시지 이름도 MyHarness 이름으로 바꿨습니다. 브라우저에 남는 workspace, artifact panel, system prompt, HTML preview 상태가 새 앱 이름과 일관되게 저장됩니다.
- 웹 세션 요청에 `clientId`를 더 명확히 붙였습니다. 이벤트 스트림, 메시지 전송, 취소, 권한 응답, 아티팩트 저장/삭제 같은 API가 현재 브라우저 클라이언트의 세션인지 확인하도록 보강했습니다.
- 브라우저가 오래 켜져 있거나 백엔드 세션이 끊겼을 때 `Unknown session` 오류에서 새 세션을 만들어 복구하는 흐름을 추가했습니다. 사용자가 입력한 메시지가 세션 단절 때문에 바로 실패하는 일을 줄이기 위한 변경입니다.
- 설정 화면에 터미널 세션 재시작 기능을 추가했습니다. 현재 세션을 강제로 닫고 같은 작업공간에서 새 백엔드 세션을 시작할 수 있어, 권한 설정 변경이나 세션 꼬임을 웹 UI 안에서 회복하기 쉬워졌습니다.
- 서버 종료, 예외, unhandled rejection, warning, 세션 재시작 과정을 런타임 로그로 남기도록 했습니다. 웹 서버가 왜 종료됐는지, 어떤 세션이 살아 있었는지 추적하기 쉬워졌습니다.
- Yolo 모드 설정을 웹 UI에 추가했습니다. 자동 실행을 켜면 새 세션이 `full_auto` 권한으로 시작하고, 끄면 기본 권한 모드로 시작해 실행 전 확인이 더 자주 뜨도록 했습니다.
- Plan Mode 전환 도구를 새로 정리했습니다. Plan Mode에 들어갈 때 이전 권한 모드를 기억하고, 나올 때 원래 모드로 되돌아가도록 해서 `plan` 상태가 설정에 남아 꼬이는 일을 줄였습니다.
- 웹에서 `/plan`을 토글할 때 작성 중이던 입력문, 첨부 이미지, 붙여넣은 긴 텍스트, 선택한 스킬 토큰을 지우지 않도록 고쳤습니다. 계획 모드만 켜려다가 초안을 잃는 일이 줄었습니다.
- 권한 요청 모달을 화면 중앙 팝업 대신 입력창 근처의 인라인 카드로 표시하도록 바꿨습니다. 답변 흐름을 보면서 허용/거부를 선택할 수 있어 작업 맥락이 덜 끊깁니다.
- 프로젝트 파일 패널을 크게 보강했습니다. 파일을 web, docs, data, code, other 유형으로 분류하고, `outputs/`와 루트 산출물 중심의 기본 목록과 전체 목록을 나눠 볼 수 있게 했습니다.
- 프로젝트 파일 목록을 폴더별로 접고 펼칠 수 있게 했습니다. 산출물이 많아져도 루트, `outputs`, 하위 폴더 단위로 묶어 확인할 수 있어 오른쪽 패널이 덜 복잡해졌습니다.
- 루트에 흩어진 결과 파일을 `outputs/` 쪽으로 정리하는 API와 UI를 추가했습니다. 같은 이름의 파일이 있으면 숫자 suffix를 붙여 덮어쓰지 않도록 했습니다.
- 답변 저장 기본 경로를 루트가 아니라 `outputs/` 아래로 바꿨습니다. 에이전트가 만든 마크다운 답변 파일이 프로젝트 최상단을 어지럽히지 않게 하려는 변경입니다.
- 아티팩트 iframe과 Markdown 안의 HTML 미리보기가 컨테이너 크기 변화를 더 잘 따라가도록 resize 메시지와 관찰 로직을 보강했습니다. 패널 폭을 바꾸거나 화면 크기가 바뀔 때 HTML 결과물이 덜 잘리고 덜 넘칩니다.
- 히스토리 정렬 로직을 분리하고 테스트했습니다. 명시적인 `created_at`이 있으면 그것을 우선하고, 오래된 세션 파일은 수정 시간보다 생성 시간을 먼저 써서 과거 대화 순서가 덜 흔들리게 했습니다.
- 저장된 히스토리와 현재 살아 있는 세션 목록을 합칠 때 같은 대화가 중복 표시되지 않도록 병합 로직을 다듬었습니다. 진행 중인 세션과 저장된 세션이 한 목록에서 더 자연스럽게 보입니다.
- 체크리스트 카드는 본문을 눌러도 접고 펼칠 수 있게 했고, 완료된 항목은 취소선 대신 `(완료)` 접두사로 표시하도록 바꿨습니다. 작은 글자와 좁은 화면에서도 완료 상태를 더 확실히 읽을 수 있게 한 조정입니다.
- POSCO 관리 Windows PC에서 `C:/POSCO_CA.crt`를 감지해 Python, Node, npm이 쓸 수 있는 CA 설정을 자동으로 잡는 지원을 추가했습니다. P-GPT 같은 사내 HTTPS 연결에서 인증서 문제로 막히는 일을 줄이려는 목적입니다.
- POSCO CA와 certifi/system CA를 합친 번들을 만드는 `scripts/build_posco_ca_bundle.py`를 추가했습니다. 별도 점검이나 배포 준비 때 같은 인증서 번들을 재사용할 수 있습니다.
- 웹 UI와 Python 런타임 테스트를 새 구조에 맞춰 보강했습니다. 세션 복구, Plan Mode 초안 보존, 프로젝트 파일 분류, 히스토리 정렬, 체크리스트 표시, 인증서 번들 같은 깨지기 쉬운 부분을 테스트로 묶었습니다.

## 2026-05-01 (Myeongcheol)

이날은 웹 화면을 실제 업무 도구처럼 오래 켜 두고 쓰기 위해, 미리보기, 설정, 진행 상태, 디자인 보조 기능을 전반적으로 다듬은 날입니다. 화면을 크게 갈아엎기 전, 기존 웹 UI에서 불편했던 부분을 한 번 더 정리했습니다.

- 아티팩트 미리보기에서 HTML, Markdown, 보고서 형태가 더 잘 보존되도록 고쳤습니다. 에이전트가 만든 결과물을 열었을 때 원래 의도한 형식이 덜 무너지고, 보고서처럼 읽을 수 있는 결과물은 보고서답게 보이도록 한 작업입니다.
- 결과물 미리보기에 전체화면 토글을 추가했습니다. 긴 HTML 보고서나 시각 자료를 오른쪽 패널 안에서만 보는 것이 답답할 때, 더 넓은 화면으로 확인할 수 있게 되었습니다.
- HTML 스트리밍 미리보기를 다듬었습니다. 답변이 생성되는 중에도 HTML 조각과 Markdown 내용이 덜 깨져 보이도록 렌더링과 스타일을 정리했습니다.
- 코드 블록, 표, 긴 문단이 섞인 답변이 화면에서 덜 답답하게 보이도록 메시지 스타일을 조정했습니다. 특히 생성 중인 답변과 완료된 답변의 표시가 더 자연스럽게 이어지도록 했습니다.
- 테마 관련 UI 상태를 정리했습니다. 색상과 화면 상태가 일부 상황에서 이전 설정처럼 보이거나 어색하게 남는 문제를 줄였습니다.
- 디자인 결과물을 만들 때 참고할 수 있는 `design-md` 스킬과 브랜드/서비스별 디자인 레퍼런스를 추가했습니다. Airbnb, Apple, Stripe, Notion, Vercel 같은 사례를 참고해 HTML 보고서나 프로토타입을 더 높은 품질로 만들기 위한 기반입니다.
- `ui-design-essence`, `visual-artifact` 같은 시각 산출물 관련 스킬 설명과 사용 흐름을 보강했습니다. 단순히 예쁜 화면을 만드는 것이 아니라, 실제 업무 보고서나 대시보드처럼 목적이 분명한 결과물을 만들기 위한 방향입니다.
- 스킬 설명 표시를 정리했습니다. 사용자가 스킬을 고를 때 내부 파일명보다 “무엇을 도와주는 기능인지”를 더 쉽게 이해하도록 표시 흐름을 다듬었습니다.
- 긴 대화에서 남은 토큰을 추정하는 방식을 개선했습니다. 작업이 길어질 때 언제 압축이 필요하고, 어느 정도 여유가 남았는지 더 현실적으로 계산하게 했습니다.
- 대화 압축 과정에서 중요한 맥락이 빠지지 않도록 보강했습니다. 긴 작업을 이어받을 때 이전 지시나 산출물 흐름을 잃어버리는 일을 줄이려는 변경입니다.
- 파일 읽기, 쓰기, 편집 도구가 보여주는 경로 표시를 정리했습니다. Windows 전체 경로와 프로젝트 안의 상대 경로가 섞여도, 사용자가 “어느 파일을 건드렸는지” 더 쉽게 알 수 있게 했습니다.
- Todo 작성 도구의 표시를 다듬었습니다. 작업 목록이 답변 속에서 어색하게 튀거나 상태가 불명확해지는 일을 줄이는 방향입니다.
- 설치 프로그램과 실행 배치 파일의 의존성 점검을 보강했습니다. 웹 UI를 켤 때 필요한 패키지나 준비 단계가 빠져 있으면 더 빨리 알아차릴 수 있게 했습니다.
- 설정 화면에 IP 기반 사용 통계 표시를 추가했습니다. 로컬 웹 서버를 여러 환경에서 켜 두거나 사내망에서 접속 상태를 확인할 때 기본적인 사용 현황을 볼 수 있게 하기 위한 기능입니다.
- 모델 선택과 추론 강도 라벨을 더 알기 쉽게 바꿨습니다. “어떤 모델을 쓰는지”, “얼마나 깊게 생각하게 할지”가 화면에서 덜 헷갈리도록 표현을 정리했습니다.
- 터미널 화면 하단의 모델/추론 강도 표시도 함께 맞췄습니다. 웹 UI와 터미널 UI가 서로 다른 말을 하는 것처럼 보이지 않게 하려는 조정입니다.
- Plan Mode가 화면 상태 갱신 중에도 유지되도록 고쳤습니다. 계획 모드로 전환해 두었는데 세션 정보가 갱신되면서 갑자기 일반 모드처럼 보이는 문제를 줄였습니다.
- 스크롤 동작을 보강했습니다. 새 답변이 오면 따라가야 할 때와 사용자가 이전 내용을 읽고 있어서 멈춰야 할 때를 더 안정적으로 나누기 위한 준비입니다.
- 상태 동기화와 스크롤 동작에 대한 테스트를 추가했습니다. 화면에서만 보이는 작은 회귀가 다시 생기지 않도록 확인 범위를 넓혔습니다.
- `superpowers` 플러그인 묶음이 들어오면서 브레인스토밍, 계획 작성, 테스트 주도 개발, 코드 리뷰 요청, 완료 전 검증 같은 작업 흐름을 스킬로 참조할 수 있게 되었습니다. MyHarness 자체 화면 기능은 아니지만, 에이전트가 일하는 방식의 선택지가 넓어진 변화입니다.

## 2026-05-03 (Myeongcheol)

이날은 큰 화면 전환을 바로 적용하기보다, 기존 웹 화면을 되돌아볼 수 있는 기준으로 남기고 새 화면 구조를 어떻게 옮길지 정리한 날입니다. 사용자가 보는 변화는 아직 작지만, 다음 날부터 이어지는 큰 웹 UI 개선을 준비한 단계입니다.

- GitHub CLI 인증 관련 작업 기록을 보강했습니다. GitHub 관련 작업을 할 때 “로그인이 안 된 것인지”, “권한이 부족한 것인지”, “명령 사용법 문제인지”를 더 빨리 구분하기 위한 기록입니다.
- 기존 웹 UI 상태를 비교 기준으로 남겼습니다. 이후 새 화면으로 바꾼 뒤에도 이전 화면에서 잘 되던 흐름을 확인할 수 있게 하기 위한 안전장치입니다.
- 새 웹 UI 전환 계획을 문서로 정리했습니다. 대화, 히스토리, 설정, 파일, 아티팩트, 진행 상태 표시를 어떤 순서로 옮길지 먼저 나눠 두어, 화면 전환 중 기능이 빠지는 일을 줄이려는 목적입니다.
- 새 화면은 처음부터 기존 화면을 지우는 방식이 아니라, 옆에서 미리 확인한 뒤 기본 화면으로 바꾸는 흐름으로 준비했습니다. 사용 중인 기능을 갑자기 잃지 않게 하기 위한 전환 방식입니다.

## 2026-05-04 (Myeongcheol)

이날은 MyHarness 웹 UI가 새 구조로 크게 바뀐 날입니다. 겉으로는 같은 로컬 웹 앱이지만, 내부적으로는 대화창, 입력창, 설정, 히스토리, 아티팩트, 작업 진행 패널이 각각 독립적으로 관리되는 형태가 되었습니다. 덕분에 화면이 복잡해져도 기능을 더 안정적으로 고치고 확장할 수 있는 기반이 생겼습니다.

- 새 웹 UI의 기본 뼈대를 만들었습니다. 왼쪽 히스토리/프로젝트 영역, 가운데 채팅, 오른쪽 아티팩트/작업 패널이 한 화면에서 더 명확하게 나뉘도록 구성했습니다.
- 기존 웹 화면은 보존하고 새 화면을 따로 띄워 확인할 수 있게 했습니다. 화면 전환 중에도 이전 방식과 비교하면서 기능 누락을 확인할 수 있게 하려는 조치입니다.
- 채팅 입력창을 새로 정리했습니다. 긴 지시문, 첨부파일, 자동완성, 멘션, 선택한 스킬이 한꺼번에 들어와도 입력 상태가 덜 흔들리게 했습니다.
- 사용자가 질문에 답해야 하는 상황을 인라인 카드로 표시하도록 다듬었습니다. 별도 팝업에 가려지는 대신, 대화 흐름 안에서 어떤 선택을 해야 하는지 더 자연스럽게 보이게 했습니다.
- 권한 요청과 확인 모달을 새 화면 구조에 맞춰 옮겼습니다. 실행 허용/거부 같은 중요한 선택이 채팅 흐름과 어긋나지 않도록 정리했습니다.
- Todo 체크리스트에서 닫기 버튼을 제거했습니다. 작업 진행 중 필요한 목록이 실수로 사라지는 일을 줄이고, 작은 dock 형태로 계속 참고할 수 있게 했습니다.
- Todo dock의 실제 표시를 브라우저에서 확인하기 위한 점검 파일도 남겼습니다. 체크리스트는 작지만 자주 보는 UI라 깨짐을 줄이는 데 신경 쓴 부분입니다.
- 답변이 생성되는 즉시 채팅 화면에 보이도록 고쳤습니다. 예전처럼 한참 기다린 뒤 한 번에 나타나는 느낌을 줄이고, 에이전트가 현재 무엇을 쓰고 있는지 더 빨리 볼 수 있게 했습니다.
- 작업 진행 패널의 순서를 고쳤습니다. 파일 생성, 도구 실행, 중간 설명, 최종 답변이 섞여 보일 때 진행 상황을 이해하기 어려워서, 화면에 표시되는 순서를 실제 작업 흐름과 맞췄습니다.
- 워크플로 미리보기 영역을 보강했습니다. 에이전트가 어떤 단계로 일하고 있는지, 어떤 결과가 생겼는지 더 읽기 쉽게 표시하는 방향입니다.
- 아티팩트 패널을 새 화면 구조에 맞게 옮겼습니다. 생성된 HTML, Markdown, 이미지, 데이터 파일을 오른쪽에서 확인하고, 채팅 안의 관련 카드와 연결되도록 정리했습니다.
- 프로젝트 파일과 히스토리 데이터를 불러오는 흐름을 새 화면에 맞게 다시 연결했습니다. 작업공간을 바꾸거나 과거 대화를 열 때 화면 상태가 덜 꼬이도록 했습니다.
- 히스토리 복원 테스트를 보강했습니다. 이전 대화를 열었을 때 워크플로 표시와 메시지 순서가 함께 복원되는지 확인하기 위한 작업입니다.
- 설정, 세션, 작업공간, 아티팩트, 메시지 전송 API를 새 화면에서 안정적으로 부를 수 있도록 정리했습니다. 사용자는 같은 버튼을 누르지만, 뒤에서는 요청과 응답의 약속을 더 명확히 한 것입니다.
- 서버 보안 테스트를 추가했습니다. 로컬 웹 서버가 파일 경로나 요청을 처리할 때 의도하지 않은 접근을 허용하지 않도록 확인 범위를 넓혔습니다.
- Slack 채널과 스트리밍 이벤트 처리도 함께 보강했습니다. 외부 채널이나 실시간 이벤트가 웹 화면과 더 잘 맞물리도록 한 안정화입니다.
- Bash 도구와 질문 도구의 메시지 처리를 다듬었습니다. 명령 실행 중 나온 정보나 사용자에게 묻는 내용이 웹 화면에서 더 자연스럽게 보이도록 했습니다.
- 웹 검색/웹 가져오기 실패 메시지를 정리했습니다. 검색 결과가 없거나 403 같은 접근 실패가 났을 때, 단순한 오류보다 상황을 이해하기 쉬운 안내가 나오도록 보강했습니다.
- P-GPT 인증 관련 설정과 테스트를 보강했습니다. 사내 연결 설정을 다루는 흐름이 새 웹 화면에서도 덜 불안정하게 이어지도록 했습니다.
- 설치 프로그램, 일반 실행 배치 파일, 개발 실행 배치 파일을 새 웹 UI 구조에 맞게 고쳤습니다. 사용자는 기존처럼 실행하되, 내부 준비와 확인 절차가 새 구조에 맞게 돌아가도록 한 작업입니다.
- React/Vite/TypeScript 기반 개발 환경을 추가했습니다. 이는 사용자에게 직접 보이는 기능은 아니지만, 이후 화면을 더 안정적으로 만들고 테스트하기 위한 기반입니다.
- 주요 화면 요소별 테스트를 추가했습니다. 입력창, 메시지 목록, 사이드바, 아티팩트 패널, 상태 reducer가 기대대로 움직이는지 확인하는 테스트가 크게 늘었습니다.
- UI 점검용 격리 스크립트를 추가했습니다. 화면 확인 중 생기는 로그, 스크린샷, 임시 브라우저 프로필이 프로젝트 루트에 흩어지지 않게 관리하기 위한 준비입니다.
- 예시 산출물로 HTML 보고서, 데이터 JSON, 노트 Markdown 파일이 `outputs/`에 남았습니다. 새 아티팩트/보고서 흐름을 실제 결과물로 확인한 흔적입니다.

## 2026-05-05 (Myeongcheol)

이날은 전날 만든 새 웹 화면을 실제 기본 화면으로 쓰기 위해 사용성과 안정성을 끌어올린 날입니다. 기술적인 전환 자체보다, 사용자가 계속 켜 두고 써도 답답하지 않고 중간에 새로고침해도 흐름을 덜 잃도록 만드는 데 집중했습니다.

- 아티팩트 패널을 더 작고 명확한 단위로 나눴습니다. 파일 목록, 미리보기, 아이콘, 채팅 안의 아티팩트 카드가 각자 역할을 갖게 되어 결과물을 찾고 확인하기 쉬워졌습니다.
- HTML/Markdown/이미지/데이터 파일이 각각 다른 방식으로 보이던 부분을 정리했습니다. 생성 결과가 어디에 있든 비슷한 규칙으로 열고 확인할 수 있게 하려는 개선입니다.
- 답변 아래 액션 버튼을 정리했습니다. 저장, 다시 보기, 관련 결과 확인 같은 후속 동작이 답변 흐름 안에서 덜 어색하게 보이도록 다듬었습니다.
- 설정 화면을 별도 컴포넌트로 분리하면서 구조를 정리했습니다. 모델, 제공자, 다운로드, 작업공간 같은 설정이 한 파일에 뒤섞이지 않게 되어 이후 수정이 쉬워졌습니다.
- 설정 화면의 다운로드 관련 상태를 실제 화면 캡처로 확인하며 다듬었습니다. 브라우저 기본 다운로드와 앱 안 미리보기 흐름이 헷갈리지 않게 하려는 작업입니다.
- 사용자 메시지 표시를 다듬었습니다. 사용자가 보낸 긴 지시문, 파일 언급, 특수 텍스트가 채팅 안에서 덜 깨지고 더 읽기 좋게 보이도록 했습니다.
- 스트리밍 중인 assistant 답변 표시를 새로 정리했습니다. 답변 생성 중, 도구 실행 중, 결과물 생성 중 상태가 한 덩어리로 뭉개지지 않게 했습니다.
- 답변 소스 패널이 바깥을 클릭하면 닫히도록 고쳤습니다. 작은 보조 패널이 화면에 계속 남아 다른 작업을 가리는 일이 줄었습니다.
- 새 메시지가 올 때 화면이 따라 내려가는 동작을 더 섬세하게 만들었습니다. 사용자가 최신 답변을 보고 있으면 자동으로 따라가고, 이전 내용을 읽고 있으면 갑자기 아래로 튀지 않게 하는 방향입니다.
- 브라우저 새로고침이나 연결 재개 후에도 진행 중이던 답변을 더 잘 복원하도록 했습니다. 실시간 답변이 끊겼다가 다시 이어질 때 기존 이벤트를 재생해, 작업 흐름이 사라져 보이는 일을 줄였습니다.
- 아티팩트도 세션 복구와 더 잘 맞물리게 했습니다. 진행 중 생성된 파일 카드와 오른쪽 미리보기 상태가 재연결 후에도 덜 어긋나도록 보강했습니다.
- 도움말 메시지와 명령 안내를 다듬었습니다. 사용자가 화면에서 가능한 명령이나 스킬 사용법을 볼 때 설명이 더 읽기 좋게 나오도록 했습니다.
- 커스텀 툴팁 레이어를 보강했습니다. 브라우저 기본 툴팁 대신 앱 안에서 통일된 방식으로 설명을 보여주기 위한 준비입니다.
- 사이드바의 히스토리와 작업공간 표시를 다듬었습니다. 과거 대화를 찾거나 현재 작업공간 상태를 확인할 때 목록이 덜 흔들리도록 했습니다.
- 프로젝트 파일 목록과 아티팩트 목록의 스크롤/접힘 동작을 확인하고 테스트했습니다. 결과물이 많아질수록 작은 UI 깨짐이 커지기 때문에 이 부분을 따로 봤습니다.
- 팀/작업자 에이전트 기능을 웹 화면에서 더 직접적으로 다룰 수 있는 버튼을 추가했습니다. 여러 에이전트를 나눠 쓰는 흐름이 터미널 전용 기능처럼 보이지 않게 한 변화입니다.
- 작업자 에이전트의 생성, 메시지 전달, 상태 갱신이 웹 세션과 더 잘 맞도록 백엔드 처리를 보강했습니다. 화면에서 누른 동작과 실제 작업자 상태가 어긋나는 일을 줄이려는 목적입니다.
- Codex/OpenAI 연결 쪽 안정성을 보강했습니다. 모델 응답, 스트리밍, 오류 처리, 테스트를 함께 손봐 웹 화면에서 발생하는 실패를 더 잘 잡을 수 있게 했습니다.
- query engine과 백엔드 호스트 처리를 보강했습니다. 사용자가 보낸 질문이 실제 엔진으로 들어가고, 결과가 다시 웹 UI로 돌아오는 흐름을 더 단단하게 만든 작업입니다.
- 프로젝트 지침을 시스템 프롬프트에 반영하는 흐름을 다듬었습니다. 작업공간의 `AGENTS.md` 같은 규칙이 누락되거나 중복되지 않게 하려는 정리입니다.
- 세션 저장과 복원 처리를 보강했습니다. 이전 작업을 다시 열 때 메시지, 진행 상태, 작업자 정보가 더 안정적으로 남도록 했습니다.
- 이미지 생성, 파일 저장, 압축, 시스템 프롬프트 관련 테스트를 추가했습니다. 웹 화면에서 보이는 기능뿐 아니라 뒤에서 결과물을 만드는 도구들도 같이 확인한 것입니다.
- UI 점검 중 생긴 임시 캡처 파일을 정리하고 ignore 규칙을 보강했습니다. 확인용 파일이 실제 기능 변경과 섞여 커밋되지 않도록 한 관리 작업입니다.
- 디자인 보조 스킬을 추가했습니다. `frontend-design`, `polish`, `tailwind-design-system`, `ui-ux-pro-max` 같은 스킬을 통해 이후 화면 개선과 시각 산출물 제작을 더 체계적으로 할 수 있게 되었습니다.
- 작업 지침도 MyHarness 현재 방식에 맞게 업데이트했습니다. 특히 UI를 만들거나 고친 뒤 실제 화면에서 깨짐, 겹침, 콘솔 오류를 확인하라는 기준이 더 분명해졌습니다.
- 전환 과정상 기존 화면을 보존한 뒤 새 화면을 검증하고, 이 시점부터 새 웹 화면을 기본으로 쓰는 흐름이 되었습니다. 사용자 입장에서는 “웹 UI가 새 구조로 교체되고, 기존 주요 기능을 계속 가져온 날”로 보면 됩니다.

## 2026-05-06 (Myeongcheol)

이날은 새 웹 화면을 기본으로 올린 뒤, 실제 운영 기준에 맞게 표현, 안전장치, 렌더링, 작업자 에이전트 흐름을 정리한 날입니다. 큰 기능을 새로 붙이기보다, 전날까지 바뀐 화면과 런타임을 오래 쓰기 좋게 다듬는 성격이 강했습니다.

- 스킬 설명의 한국어 표시를 더 정리했습니다. 원본 스킬 파일을 직접 번역하기보다, UI에서 보여주는 설명과 매핑을 다듬어 사용자가 기능을 고를 때 더 이해하기 쉽게 했습니다.
- 명령 도움말 화면의 스킬 설명 표시를 보강했습니다. 남아 있던 영어 설명이나 내부 표현이 사용자에게 그대로 보이는 일을 줄였습니다.
- 파일 변경 작업이 동시에 겹칠 때를 대비해 mutation lock을 추가했습니다. 여러 작업이나 도구가 같은 파일을 한꺼번에 건드리려 할 때 충돌과 꼬임을 줄이기 위한 안전장치입니다.
- 작업자 에이전트 관련 도구 설명과 이벤트 처리를 보강했습니다. 에이전트를 만들고, 메시지를 보내고, 작업을 중지하고, 팀을 만들거나 삭제하는 흐름이 더 명확해졌습니다.
- 작업 업데이트 도구를 보강했습니다. 진행 중인 작업의 상태를 바꾸거나 설명을 갱신하는 흐름이 더 안정적으로 이어지게 했습니다.
- 비동기 에이전트 표시를 보강했습니다. 백그라운드에서 돌아가는 작업자 상태가 웹 화면과 런타임에서 덜 어긋나도록 했습니다.
- Markdown 메시지 안의 Mermaid 다이어그램 렌더링을 추가했습니다. 답변 안에 흐름도나 구조도가 들어오면 글자만 보이는 대신 실제 다이어그램으로 확인할 수 있게 되었습니다.
- Mermaid 렌더링 테스트를 추가했습니다. 다이어그램 표시가 다시 깨지거나 빈 화면으로 나오는 문제를 잡기 위한 테스트입니다.
- Markdown 렌더링 전반을 다듬었습니다. 긴 문서, 코드, 다이어그램, 링크, 아티팩트 카드가 섞인 답변이 더 안정적으로 보이도록 했습니다.
- 모달과 사이드바 테스트를 보강했습니다. 설정창, 확인창, 히스토리/작업공간 목록처럼 자주 열고 닫는 화면이 전환 후에도 제대로 동작하는지 확인했습니다.
- 작업공간 데이터 로딩 테스트를 보강했습니다. 프로젝트 파일, 세션 목록, 설정 상태를 불러오는 과정에서 빠지거나 중복되는 값이 없는지 확인하려는 목적입니다.
- 서버 보안 테스트를 다시 보강했습니다. 새 웹 화면에서 추가된 경로와 API가 안전하게 처리되는지 확인 범위를 넓혔습니다.
- AGENTS 작업 지침을 현재 운영 기준에 맞게 다시 정리했습니다. OCR 요청 처리, UI 작업 후 화면 점검, 슬림한 UI 밀도, 커스텀 툴팁 사용, 점검 산출물 보관 위치 같은 기준이 더 분명해졌습니다.
- 프로젝트 지침과 시스템 프롬프트 연결을 보강했습니다. 작업공간 규칙이 에이전트에게 더 일관되게 전달되도록 한 안정화입니다.
- React 웹 UI와 Python 런타임 테스트를 함께 보강했습니다. 새 화면 구조가 겉보기만 바뀐 것이 아니라, 메시지 전송과 작업 실행까지 이어지는 전체 흐름이 유지되는지 확인하기 위한 작업입니다.

## 2026-05-08 (Myeongcheol)

이날은 새 React 웹 UI를 실제 작업 흐름에 맞춰 더 안정화하고, HTML/Markdown 산출물을 보는 경험을 크게 다듬은 날입니다. 대화가 길어지고 도구 실행 결과가 섞일수록 화면이 흐트러지기 쉬웠기 때문에, 메시지 렌더링, 워크플로 표시, 런타임 설정 전달, 아티팩트 카드 표시를 함께 정리했습니다.

- 런타임 설정을 웹 UI에서 더 안정적으로 읽고 적용하도록 보강했습니다. 모델, 권한, 작업공간 관련 값이 화면과 백엔드 사이에서 어긋나는 일을 줄이기 위한 작업입니다.
- Composer 입력창과 사이드바 상태 처리를 다듬었습니다. 긴 입력, 선택 상태, 런타임 환경 표시가 섞여도 화면이 덜 흔들리도록 했습니다.
- 워크플로 패널 표시를 보강했습니다. 에이전트가 어떤 파일을 만들고, 어떤 도구를 실행하고, 어떤 중간 결과를 내고 있는지 더 읽기 좋게 정리했습니다.
- 메시지 상태 reducer와 세션 훅 테스트를 추가했습니다. 스트리밍 답변, 세션 복원, 워크플로 이벤트가 계속 들어오는 상황에서도 UI 상태가 안정적으로 유지되는지 확인하기 위한 기반입니다.
- Python 쪽 CLI, 명령 등록, 설정, 플러그인/스킬 로더, 백엔드 호스트 처리를 함께 보강했습니다. 웹 UI에서 누른 동작이 실제 런타임과 더 일관되게 이어지도록 한 안정화입니다.
- Markdown 메시지 안에서 여러 블록이 이어질 때 간격이 어색하거나 붙어 보이는 문제를 고쳤습니다. 답변이 문단, 코드, 다이어그램, 아티팩트 카드로 나뉘어도 읽는 흐름이 자연스럽게 이어지도록 했습니다.
- 스트리밍 중인 답변의 Markdown 렌더링을 다듬었습니다. 생성 중에는 덜 완성된 문장이 들어오고 구조가 계속 바뀌기 때문에, 완료 전후 간격과 카드 배치가 갑자기 튀지 않게 정리했습니다.
- assistant 답변 아래에 표시되는 아티팩트 카드와 HTML/Markdown 미리보기 연결을 다듬었습니다. 생성 결과를 찾고 여는 흐름이 더 자연스러워졌습니다.
- Mermaid와 Markdown 렌더링 테스트를 크게 보강했습니다. 답변 안의 다이어그램, 일반 Markdown, 아티팩트 카드가 함께 있을 때도 깨지지 않도록 회귀 테스트를 늘렸습니다.
- 웹 UI와 Python 런타임 양쪽의 테스트 범위를 넓혔습니다. 화면에서 보이는 문제와 백엔드 이벤트 처리 문제가 서로 영향을 주기 때문에, 한쪽만 확인하지 않도록 한 작업입니다.

## 2026-05-09 (Myeongcheol)

이날은 HTML 보고서와 Mermaid 다이어그램을 “생성된 파일”이 아니라 “편집하고 검토할 수 있는 산출물”로 다루기 위한 변화가 많았습니다. 특히 오른쪽 아티팩트 패널에서 HTML 본문을 직접 보거나 고치고, AI 편집 요청을 남기고, 버전별 결과를 확인하는 흐름이 크게 확장되었습니다.

- HTML 아티팩트 패널을 대폭 보강했습니다. 오른쪽 미리보기 안에서 HTML 결과를 더 안정적으로 열고, 원본/미리보기/편집 상태를 더 명확하게 구분할 수 있게 했습니다.
- HTML 본문 편집과 AI 편집 요청 흐름을 추가했습니다. 사용자가 결과물 일부를 고르거나 문서 전체에 지시를 남긴 뒤, 새 버전의 HTML 산출물로 이어지게 하는 기반입니다.
- AI 편집 결과는 바로 기존 화면을 갈아끼우지 않고, 새 버전이 준비된 뒤 전환되도록 정리했습니다. 편집 중인 보고서가 갑자기 흔들려 보이는 문제를 줄이기 위한 방식입니다.
- 아티팩트 버전 표시와 전환 흐름을 보강했습니다. `v1`, `v2`처럼 생성된 결과를 비교하고 다시 열 수 있어, 보고서 수정 과정을 추적하기 쉬워졌습니다.
- Mermaid 다이어그램이 들어간 HTML 산출물에 확대/축소와 초기화 컨트롤을 넣었습니다. 다운로드한 단일 HTML에서도 큰 다이어그램을 더 편하게 살펴볼 수 있게 하려는 개선입니다.
- 보고서/시각 산출물 관련 지침을 보강했습니다. HTML 보고서에서 차트, 다이어그램, 아이콘을 어떤 상황에 쓰면 좋은지 더 분명히 하여 결과물 품질을 높이는 방향입니다.
- 브라우저 기본 `title` 툴팁 대신 앱의 커스텀 툴팁 스타일을 쓰는 기준을 다시 문서화했습니다. MyHarness 화면의 반응과 시각 톤을 일관되게 유지하기 위한 규칙입니다.
- 파일 도구와 스킬 출력에서 로컬 전체 경로가 과하게 드러나지 않도록 표시 방식을 다듬었습니다. 사용자가 보는 로그와 카드에서는 프로젝트 안의 의미 있는 경로가 더 잘 보이게 했습니다.
- UTF-8 인코딩 가드 스크립트를 추가했습니다. 한글이 포함된 파일을 Windows PowerShell 경로에서 잘못 재저장해 깨뜨리는 일을 빨리 잡기 위한 안전장치입니다.
- 코드 그래프 생성물과 UI 점검 산출물이 Git 변경 목록에 섞이지 않도록 ignore 규칙을 보강했습니다.
- 아티팩트 패널, 서버 보안, 워크플로 미리보기, Mermaid 렌더링, UTF-8 가드 테스트를 함께 늘렸습니다. 기능 확장 폭이 컸던 만큼 회귀를 잡는 표면도 같이 넓힌 것입니다.

## 2026-05-10 (Myeongcheol)

이날은 React 웹 UI 전환을 마무리하면서 오래된 레거시 웹 파일을 걷어내고, 현재 쓰는 화면과 런타임 기준으로 기능을 정리한 날입니다. 동시에 Mermaid 사전 점검, 파일 도구 표시, 보고서 작성 지침 같은 실제 사용 중 자주 닿는 부분을 보강했습니다.

- 이전 레거시 웹 UI 파일과 테스트를 제거했습니다. `legacy-index.html`, 예전 `modules/*.js`, `script.js`, 구형 웹 테스트를 정리해 현재 React/Vite 기반 화면이 기준이 되도록 했습니다.
- 웹 서버와 실행 흐름을 React UI 중심으로 정리했습니다. 더 이상 두 화면 구조를 같이 유지하면서 생기는 혼란을 줄이고, 실행 경로를 현재 화면에 맞췄습니다.
- 아티팩트 패널과 HTML 미리보기 UI를 계속 다듬었습니다. 본문 편집, 버전 전환, 미리보기 상태, 버튼 배치가 더 일관되게 보이도록 정리했습니다.
- Mermaid 사전 점검 도구를 추가했습니다. 답변이나 산출물에 들어가는 Mermaid 문법을 미리 확인해, 화면에서는 빈 다이어그램이나 깨진 블록으로 보이는 일을 줄이려는 기능입니다.
- Mermaid 검증 스크립트와 관련 테스트를 추가했습니다. 다이어그램 렌더링이 실제 웹 화면과 HTML 산출물에서 안정적으로 유지되는지 확인하기 위한 보강입니다.
- 파일 쓰기/편집 도구의 출력 표시를 다듬었습니다. 사용자가 보는 도구 결과에서 로컬 절대경로보다 실제 작업 맥락에 맞는 경로가 보이도록 했습니다.
- 작업자 에이전트 도구 설명과 응답 처리를 정리했습니다. 여러 에이전트를 호출하거나 상태를 확인할 때 UI와 로그가 더 이해하기 쉬워졌습니다.
- 사이드바, 설정 모달, 인라인 질문 카드, Swarm 버튼 등 작은 화면 요소를 현재 디자인 밀도에 맞게 다듬었습니다. 새 React 화면 안에서 각 부품이 따로 튀지 않게 맞춘 작업입니다.
- 보고서 작성 지침에 의미형 아이콘 사용 기준을 추가했습니다. HTML 보고서에서 섹션 표식, KPI, 리스크, 권고처럼 스캔성이 좋아지는 지점에 아이콘을 절제해서 쓰도록 한 변화입니다.
- README에서 더 이상 현재 흐름과 맞지 않는 설명을 줄였습니다. 문서가 실제 실행 방식과 덜 어긋나도록 정리한 것입니다.
- 설정, CLI, React 런처, 도구, 프롬프트, 웹 컴포넌트 테스트를 함께 보강했습니다. 레거시 파일을 걷어낸 뒤에도 현재 화면과 런타임이 기대대로 이어지는지 확인하기 위한 작업입니다.

## 2026-05-11 (Myeongcheol)

이날은 새 React 웹 UI를 오래 켜 둔 상태에서 세션 상태가 다시 재생될 때, 사용자가 실제로 주고받은 대화와 진행 상태가 서로 밀어내지 않도록 안정화한 날입니다. 동시에 앞으로 리팩터링과 리뷰를 더 안전하게 하기 위한 코드 그래프 기반도 준비했습니다.

- 상태 replay 과정에서 이전 작업 진행 이벤트가 사용자 메시지나 대화 기록을 밀어내는 문제를 줄였습니다. 브라우저를 새로고침하거나 세션을 다시 이어받을 때, 실제 대화 흐름이 더 안정적으로 남습니다.
- 아티팩트 패널, 사이드바, 워크플로 패널의 상태 복원 흐름을 보강했습니다. 생성된 파일, 진행 단계, 히스토리 목록이 재연결 뒤에도 덜 어긋나도록 맞춘 작업입니다.
- 설정과 런타임 환경 전달을 다시 점검했습니다. 웹 UI에서 고른 provider, 모델, 권한, 작업공간 값이 백엔드 작업 세션으로 더 일관되게 전달되도록 정리했습니다.
- 작업자 에이전트와 도구 실행 이벤트가 화면에 표시되는 방식을 다듬었습니다. 여러 에이전트나 긴 도구 실행이 섞여도 현재 무엇이 진행 중인지 파악하기 쉽게 하려는 안정화입니다.
- 코드 그래프 MCP를 설치하고 프로젝트 그래프를 만들었습니다. 이후 리팩터링이나 영향 범위 검토 때 단순 검색만 보지 않고, 호출 관계와 테스트 관계를 함께 확인할 수 있는 기반입니다.
- 서버 보안, 세션 replay, reducer, 사이드바, 메시지 목록, 아티팩트 패널 테스트를 함께 보강했습니다. 화면 복원과 상태 동기화처럼 다시 깨지기 쉬운 부분을 테스트로 묶었습니다.

## 2026-05-12 (Myeongcheol)

이날은 대화 기록을 불러오고 긴 보고서나 AI 편집 작업을 진행할 때, 화면과 백엔드 상태가 더 차분하게 이어지도록 고친 날입니다. 특히 “과거 대화를 열었는데 내용이 비거나 다른 세션처럼 보이는” 종류의 문제를 줄이는 데 초점을 맞췄습니다.

- 대화 히스토리 로딩 문제를 고쳤습니다. 저장된 세션과 현재 작업공간 데이터를 불러올 때 누락되거나 잘못 합쳐지는 경우를 줄여, 과거 작업을 다시 여는 흐름이 안정적입니다.
- 세션 저장과 복원 로직을 보강했습니다. 메시지, 진행 상태, 작업 결과가 파일에 남고 다시 읽히는 과정에서 순서와 소속 세션이 덜 흔들리도록 정리했습니다.
- 긴 보고서 생성 진행 상태를 더 안정적으로 표시하도록 고쳤습니다. 보고서 작성 중 단계별 진행이 과하게 누락되거나 잘못 이어지는 문제를 줄였습니다.
- AI 편집 진행 표시와 출력 크기 제한을 보강했습니다. HTML/Markdown 산출물을 AI로 고치는 흐름에서 진행 상태가 더 명확해지고, 과도하게 큰 출력이 화면과 저장 흐름을 흔드는 일을 줄였습니다.
- 파일 쓰기 도구와 긴 보고서 도구의 출력 처리를 다듬었습니다. 사용자가 보는 결과 요약은 더 다루기 쉬워지고, 내부적으로는 테스트 가능한 경계가 더 분명해졌습니다.
- 세션 저장, 긴 보고서, 서버 보안, 워크스페이스 데이터, React 아티팩트 패널 테스트를 보강했습니다. 히스토리와 산출물 흐름을 함께 검증하려는 작업입니다.

## 2026-05-13 (Myeongcheol)

이날은 기능을 새로 크게 늘리기보다, 복잡해진 코드에서 “고쳐도 잘 안 고쳐지는” 지점을 줄이기 위한 소규모 리팩터링을 진행한 날입니다. public API와 화면 동작은 유지하고, 한 파일이나 한 함수가 너무 많은 책임을 갖던 부분을 먼저 나눴습니다.

- 웹 상태 reducer의 `backend_event` 처리를 별도 helper 함수로 분리했습니다. 히스토리 복원, 워크플로 도구 이벤트, 일반 백엔드 이벤트가 한 거대한 분기 안에 섞여 있던 구조를 줄여, UI 상태 버그를 추적하기 쉬워졌습니다.
- Markdown 메시지의 Mermaid 정규화 로직을 `markdownMermaid.ts`로 옮겼습니다. Markdown 렌더링 파일 안에 다이어그램 보정 규칙이 길게 섞여 있던 부분을 분리해, Mermaid 회귀 테스트와 함께 관리하기 쉬운 형태가 되었습니다.
- `ReactBackendHost`의 provider, effort, model 선택지 생성 로직을 순수 helper로 분리했습니다. 비동기 세션 처리와 UI 옵션 생성이 덜 섞이게 되어, 모델 선택 관련 변경을 더 안전하게 볼 수 있습니다.
- 명령 registry에서 `/autopilot`과 `/ship` 등록 로직을 helper factory로 빼냈습니다. 명령 이름과 동작은 유지하면서, `create_default_command_registry`가 모든 handler를 직접 품고 있던 부담을 조금 줄였습니다.
- Composer, 아티팩트 패널, 워크플로 패널, 실행 스크립트 주변도 유지보수하기 쉽게 정리했습니다. 사용자에게 보이는 큰 변화보다는 이후 수정이 덜 위험해지는 내부 정리에 가깝습니다.
- Python 명령 registry와 React backend 테스트, 웹 reducer와 Mermaid 테스트, typecheck, UTF-8 가드, 실제 웹 화면 점검을 통과시켰습니다. 데스크톱과 모바일 화면 모두 콘솔 오류와 큰 레이아웃 깨짐 없이 확인했습니다.

## 2026-05-14 (Myeongcheol)

이날은 MyHarness가 사무계 산출물을 더 잘 만들고 점검할 수 있도록, 문서·보고서·프레젠테이션 계열 스킬과 운영 보조 도구를 크게 보강한 날입니다. 동시에 자동 학습된 스킬이 UI와 프롬프트 안에서 너무 장황하게 보이지 않도록 정리해, 스킬 목록을 실제 업무에 쓰기 쉬운 방향으로 다듬었습니다.

- A4 가로형 HTML 보고서 스킬을 추가했습니다. 인쇄와 공유를 염두에 둔 보고서 산출물을 만들 때 레이아웃, 표, 차트, 섹션 구성을 더 일관되게 잡을 수 있게 하려는 기능입니다.
- PPTX 작성 스킬을 추가했습니다. 슬라이드 덱을 만들거나 기존 템플릿을 활용하는 흐름을 에이전트가 더 구조적으로 처리할 수 있도록, 참고 문서와 검사 스크립트, Node 기반 실행 환경을 함께 넣었습니다.
- 스프레드시트 분석 스킬을 추가했습니다. 업무용 workbook을 점검하고 요약하는 흐름을 별도 스킬로 다룰 수 있게 되어, 표 데이터 기반 보고와 검토 작업의 선택지가 늘었습니다.
- 스킬 평가 스킬을 추가했습니다. 새로 만든 스킬이나 가져온 스킬이 형식과 품질 기준을 잘 지키는지 확인하는 내부 점검 흐름입니다.
- 법무 검토용 lite 플러그인을 추가했습니다. 계약, 개인정보, 지식재산/오픈소스, 고용, 분쟁, 제품 표시/광고, AI 거버넌스 같은 법무 검토 초안을 업무 관점에서 정리할 수 있는 스킬 묶음입니다.
- 문서·프레젠테이션 스킬을 OpenAI 계열 에이전트에서도 바로 고를 수 있도록 agent 설정을 추가했습니다. 사용자가 스킬을 찾거나 호출할 때 특정 런타임에 묶여 보이는 일을 줄이려는 목적입니다.
- MCP HTTP 클라이언트 흐름을 보강했습니다. 원격 도구 연결에서 응답 형식과 세션 처리를 더 안정적으로 다루도록 고쳐, 새 스킬·플러그인 도구를 붙일 때 실패 지점을 줄였습니다.
- 시스템 프롬프트와 스킬 표시 로직을 정리했습니다. 직접 추가한 업무용 스킬은 잘 드러나게 하고, 자동 생성된 `learned-*` 스킬은 핵심 기능 목록을 흐리지 않도록 표현을 다듬었습니다.
- 자동 학습 스킬의 signature와 가지치기 로직을 개선했습니다. 실패한 웹 검색/웹 fetch나 반복 실행 명령에서 배운 패턴이 너무 길거나 중복된 이름으로 쌓이지 않도록 정리했습니다.
- 명령 도움말 화면에서 스킬 설명이 더 읽기 좋게 보이도록 고쳤습니다. 자동 학습 스킬이 섞여도 목록이 과하게 길어지거나 설명이 내부 로그처럼 보이는 문제를 줄였습니다.
- 개발용 웹 런처와 UI 점검 스크립트를 조금 더 안정화했습니다. React 개발 서버와 백엔드 서버를 같이 띄우는 흐름이 현재 프로젝트 구조와 맞게 이어지도록 정리했습니다.
- 자동 학습 서비스, 시스템 프롬프트, MCP HTTP flow, A4 보고서 스킬, Windows 개발 런처 관련 테스트를 보강했습니다. 새 스킬을 많이 넣은 만큼, 표시와 연결, 실행 준비가 다시 깨지지 않도록 확인 범위를 넓혔습니다.

## 2026-05-15 (Myeongcheol)

이날은 보고서와 시각 산출물 지침을 한 번 더 다듬고, React 웹 화면에서 산출물·프로젝트·세션 상태가 더 안정적으로 이어지도록 정리한 날입니다. 사용자가 결과물을 열고 다시 이어 작업할 때 불필요한 내부 경로나 불안정한 상태가 덜 드러나게 하는 데 초점이 있었습니다.

- `visual-artifact` 스킬의 보고서 디자인 지침을 보강했습니다. HTML 보고서에서 차트, 아이콘, 레이아웃을 어느 정도 적극적으로 고려해야 하는지 더 분명해졌습니다.
- A4 가로형 보고서와 일반 시각 산출물의 역할을 더 명확히 했습니다. 인쇄형 보고서와 화면형 HTML 산출물을 상황에 맞게 고르기 쉬워졌습니다.
- 아티팩트 패널과 사이드바 상태 처리를 다듬었습니다. 생성된 파일, 선택된 프로젝트, 열린 산출물이 화면에서 더 일관되게 이어지도록 한 안정화입니다.
- 세션 저장과 백엔드 연결 흐름을 보강했습니다. 웹 UI에서 진행하던 작업이 저장·복원될 때 산출물과 메시지 상태가 덜 어긋나도록 했습니다.
- 웹 서버와 개발 런처 관련 경로 처리를 정리했습니다. UI 점검 스크립트와 개발 서버 실행이 현재 프로젝트 구조 안에서 더 예측 가능하게 동작하도록 했습니다.
- 명령 registry, 시스템 프롬프트, 세션 백엔드, React backend, 서버 보안 테스트를 함께 보강했습니다. 보고서 지침 변경이 실제 화면과 런타임 흐름에도 잘 이어지는지 확인하기 위한 작업입니다.

## 2026-05-16 (Myeongcheol)

이날은 실행 환경과 스킬 라우팅을 정리하면서, 더 이상 현재 MyHarness 흐름과 맞지 않는 레거시 코드를 크게 덜어낸 날입니다. 동시에 폴더별 포트 설정과 복원 로딩 표시를 다듬어, 여러 작업 폴더에서 웹 UI를 켜는 경험을 더 안정적으로 만들었습니다.

- 웹 런처의 포트를 폴더별로 설정할 수 있게 했습니다. `myharness.local.env.example`를 추가하고 실행 스크립트와 Vite 설정을 맞춰, 여러 프로젝트를 동시에 다룰 때 포트 충돌을 줄였습니다.
- `myharness.local.env` 기반 로컬 설정 흐름을 정리했습니다. 개인 실행값은 로컬 파일로 분리하고, 예시 파일은 문서화된 기본값으로 남기는 방식입니다.
- 대화 복원 중 상태 pill이 너무 빨리 깜박이는 문제를 줄였습니다. 복원 로딩 표시가 짧은 작업에는 덜 튀고, 실제로 기다려야 하는 상황에서만 더 자연스럽게 보이도록 했습니다.
- 설정 모달, 사이드바, Todo dock 같은 작은 UI 요소를 현재 React 화면 밀도에 맞게 다듬었습니다. 상태 표시와 버튼 배치가 덜 무겁고 덜 산만해졌습니다.
- 레거시 Ohmo 패키지와 오래된 채널 연동 코드를 제거했습니다. 현재 MyHarness 실행 경로와 맞지 않는 큰 코드 덩어리를 걷어내어 유지보수 표면을 줄였습니다.
- 예전 랜딩/설명용 이미지 자산과 관련 문서를 정리했습니다. 현재 제품 설명과 실행 흐름에 맞지 않는 자료가 저장소를 흐리지 않도록 한 정리입니다.
- `frontend-design`, `ui-design-essence`, `visual-artifact`, `html-a4-landscape-report`의 역할을 다시 나눴습니다. 앱 UI 작업과 보고서/문서형 HTML 산출물 작업이 프롬프트에서 섞이지 않게 하기 위한 조정입니다.
- 스킬 표시 이름과 설명 처리, 시스템 프롬프트, 프로젝트 지침 연결을 보강했습니다. 사용자가 스킬을 고를 때 파일 경로나 내부 분류보다 실제 업무 목적이 더 잘 보이도록 했습니다.
- 포트 설정, Windows 런처, 복원 상태 표시, 스킬 표시, 시스템 프롬프트, 서버 보안 테스트를 함께 늘렸습니다. 큰 정리 뒤에도 현재 웹 실행 경로가 유지되는지 확인했습니다.

## 2026-05-17 (Myeongcheol)

이날은 실제 대화 화면에서 스트리밍 답변, 워크플로 진행 표시, 도움말, 아티팩트 작업 흐름을 더 부드럽게 만드는 데 집중한 날입니다. 특히 긴 작업을 볼 때 “지금 무엇을 하고 있는지”와 “화면이 왜 움직이는지”가 더 자연스럽게 느껴지도록 다듬었습니다.

- YouTube 자막 기반 분석 지침과 보조 스크립트를 추가했습니다. `insane-search` 스킬에서 영상 내용을 다룰 때 자막을 우선 활용하고, 필요한 경우 `yt-dlp` 기반 메타데이터·자막 흐름을 더 명확히 따를 수 있게 했습니다.
- 도움말 메시지와 명령 안내 화면을 크게 보강했습니다. 명령 설명, 버튼, 복사 동작, 사용 가능한 기능 목록이 더 읽기 쉬워지고 테스트 범위도 넓어졌습니다.
- 아티팩트 패널의 복사·미리보기·선택 흐름을 다듬었습니다. HTML/Markdown 산출물을 열고 필요한 내용을 가져가는 과정이 더 명확해졌습니다.
- 워크플로 진행 표시가 실제 작업 내용을 더 잘 반영하도록 고쳤습니다. 단순한 고정 퍼센트나 일반 문구보다, 현재 단계와 세부 작업이 화면에 더 자연스럽게 나타나도록 한 변화입니다.
- 스트리밍 텍스트 렌더러를 분리하고 정리했습니다. 답변이 생성되는 동안 Markdown, Mermaid, 일반 텍스트가 섞여도 화면이 갑자기 튀거나 완료 시점에 크게 흔들리는 일을 줄였습니다.
- 메시지 자동 따라가기 동작을 보강했습니다. 긴 답변, 워크플로 패널, 세션 복원 상태가 섞여도 사용자가 보고 있는 위치와 새로 들어오는 내용 사이의 움직임이 더 부드러워졌습니다.
- 시작 프롬프트 컴포넌트를 추가했습니다. 빈 대화나 새 작업 진입 시 사용자가 바로 시도할 수 있는 업무형 요청을 더 깔끔하게 보여주기 위한 화면 요소입니다.
- 경영기획본부용 예시 플러그인을 추가했습니다. 경영회의 브리핑, 사업계획, 전략 시나리오, 재무·투자·ESG 관련 업무 흐름을 스킬 단위로 실험할 수 있는 기반입니다.
- 자동 학습 스킬의 일반화 지침을 보강했습니다. 실패 패턴이나 반복 명령을 학습할 때 너무 좁고 긴 설명으로 쌓이지 않도록 학습 서비스와 생성되는 스킬 문구를 다듬었습니다.
- 사이드바, 설정 모달, 툴팁, 워크플로 패널, 메시지 목록, reducer, 세션 저장, 학습 서비스, React backend 테스트를 함께 보강했습니다. UI 감각 개선과 런타임 상태 변경이 다시 어긋나지 않도록 확인 범위를 넓혔습니다.

## 2026-05-20 (Myeongcheol)

이날은 PGPT 계열 스트리밍과 사용자 통계·설정 표시를 안정화하고, MyHarness 작업공간 지침을 실제 사용 흐름에 맞게 다시 다듬은 날입니다.

- provider 응답 스트리밍이 중간에 조용히 멈추거나 상태 표시가 어색하게 이어지는 문제를 줄였습니다. 긴 응답을 받을 때 화면이 더 꾸준하게 갱신되도록 한 안정화입니다.
- 웹 설정 화면의 사용자 통계 저장과 상세 표시를 보강했습니다. 설정값과 사용 현황이 서버를 오가며 덜 어긋나도록 정리했습니다.
- MyHarness 작업공간 지침과 시스템 프롬프트를 정리했습니다. UI 점검, 인코딩, 업무용 에이전트 성격 같은 프로젝트 운영 기준이 현재 방식과 더 잘 맞게 이어지도록 했습니다.
- API 키와 런타임 전달, OpenAI 호환 클라이언트, query engine, 서버 보안 테스트를 함께 보강했습니다. 스트리밍과 설정 변경이 실제 실행 경로에서 다시 깨지지 않게 하려는 확인입니다.

## 2026-05-21 (Myeongcheol)

이날은 React 웹 화면의 레이아웃과 스트리밍 업데이트를 안정화한 날입니다. 긴 작업 중 대화, 아티팩트, 워크플로 패널이 동시에 움직여도 화면이 덜 흔들리도록 다듬었습니다.

- 앱 셸, 사이드바, 채팅 패널, 입력창, 워크플로 패널의 배치 안정성을 보강했습니다. 창 크기나 상태 변화에 따라 주요 영역이 불필요하게 밀리는 문제를 줄였습니다.
- 워크플로 이벤트를 합치는 전용 로직을 추가했습니다. 비슷한 진행 이벤트가 빠르게 들어와도 화면에는 더 읽기 좋은 흐름으로 보이게 하려는 개선입니다.
- Composer와 메시지 목록의 스트리밍 표시를 다듬었습니다. 답변이 생성되는 동안 입력 영역과 대화 영역의 상태가 더 자연스럽게 유지됩니다.
- 앱 셸, Composer, 메시지 목록, backend session hook, workflow event coalescer 테스트를 추가·보강했습니다. 화면 상태가 작은 이벤트 순서 차이에 흔들리지 않도록 검증 범위를 넓혔습니다.

## 2026-05-22 (Myeongcheol)

이날은 사무계 업무에서 자주 쓰는 분석용 MCP와 서브에이전트 프리셋을 크게 늘리고, 웹 UI에서 모델·도구 선택 흐름이 더 일관되게 이어지도록 정리한 날입니다.

- SQLite 분석 MCP 예제 구성을 추가했습니다. 샘플 데이터, 로더, MCP 서버 설정을 넣어 표 데이터 질의와 분석 도구 연결을 실험하기 쉬워졌습니다.
- 업무용 서브에이전트 프리셋 플러그인을 추가했습니다. 리서처, 보고서 작성자, 비용 분석가, KPI 분석가, 팩트체커, 문서 다듬기, 표·차트 설계자 등 사무계 역할을 더 빠르게 호출할 수 있게 했습니다.
- 서브에이전트에 현재 런타임 모델 선택이 더 잘 전달되도록 고쳤습니다. 웹 UI에서 고른 모델과 실제로 spawned agent가 쓰는 모델이 어긋나는 상황을 줄였습니다.
- 명령 도움말과 Composer의 MCP·서브에이전트 제안 표시를 보강했습니다. 사용자가 어떤 도구나 역할을 부를 수 있는지 더 직접적으로 확인할 수 있게 하려는 개선입니다.
- 플러그인 로더, agent definition, 프롬프트 context, React backend, 명령 registry 테스트를 함께 늘렸습니다. 새 MCP와 프리셋이 UI 표시와 실제 실행 경로에 모두 연결되는지 확인했습니다.

## 2026-05-23 (Myeongcheol)

이날은 긴 보고서 작성과 아티팩트 작업 흐름을 더 실제 사용에 가깝게 다듬은 날입니다. 생성 중인 보고서의 진행 상황, 미리보기, 파일 작업 결과가 화면에서 더 이해하기 쉽게 이어지도록 했습니다.

- 긴 보고서 생성 과정을 워크플로 패널에서 더 세밀하게 보여주도록 보강했습니다. 단계별 진행, 산출물 준비, 완료 상태가 단순 로그보다 읽기 좋은 흐름으로 표시됩니다.
- 긴 보고서 미리보기를 스트리밍 tail 형태로 보여주도록 했습니다. 전체 결과가 끝날 때까지 기다리지 않아도 마지막으로 생성 중인 내용을 더 자연스럽게 확인할 수 있습니다.
- 아티팩트 카드와 미리보기, 사용자 메시지 안의 파일 참조 표시를 다듬었습니다. 생성된 파일을 찾고 열고 복사하는 흐름이 더 분명해졌습니다.
- 파일 도구와 long report 도구의 출력·상태 처리를 보강했습니다. 도구 실행 결과가 화면에서는 과하게 장황하지 않고, 내부적으로는 다음 단계와 연결되기 쉽게 정리되었습니다.
- 프로젝트 파일 툴바와 Tooltip, Swarm 버튼 등 작은 UI 요소의 간격을 조정했습니다. 반복해서 쓰는 작업 화면에서 버튼과 도구가 덜 답답하게 보이도록 한 밀도 조정입니다.
- 생성 산출물이 Git 변경 목록에 섞이지 않도록 ignore 규칙을 보강했습니다.
- workflow timeline, project file toolbar, server security, long report, React backend, query engine 테스트를 함께 늘렸습니다. 긴 작업 진행 표시와 산출물 흐름이 다시 흔들리지 않도록 검증했습니다.

## 2026-05-24 (Myeongcheol)

이날은 워크플로 완료 처리와 아티팩트 참조 인식을 안정화한 날입니다. 보고서나 표가 섞인 Markdown을 다룰 때, 실제 첨부·산출물과 단순 텍스트 링크를 더 잘 구분하도록 했습니다.

- 워크플로에서 아티팩트 생성 완료 상태가 더 정확히 반영되도록 고쳤습니다. 생성이 끝났는데도 진행 중처럼 남거나, 완료 카드가 늦게 정리되는 문제를 줄였습니다.
- 미리보기 영역의 스크롤 동작을 다듬었습니다. 새 내용이 들어오거나 완료 상태로 바뀔 때 사용자가 보고 있던 위치가 덜 어색하게 움직입니다.
- Markdown 표 안의 artifact ref는 실제 아티팩트 카드처럼 처리하지 않도록 고쳤습니다. 표 데이터 안에 경로나 참조 문구가 들어간 경우 UI가 잘못된 산출물로 인식하는 일을 줄였습니다.
- Todo dock과 워크플로 패널의 진행 표시를 보강했습니다. 긴 보고서나 단계형 작업의 현재 상태가 더 간결하게 보이도록 한 정리입니다.
- `visual-artifact` 지침과 샘플 데이터 로딩 흐름을 보강했습니다. 시각 HTML 산출물과 분석 데이터 실험이 현재 업무 흐름에 더 잘 맞도록 했습니다.
- 아티팩트 참조 파서, reducer, MessageList, Composer, React backend 테스트를 함께 보강했습니다.

## 2026-05-25 (Myeongcheol)

이날은 관리자 모드와 스킬 구성을 정리하고, 웹/검색 계열 기능을 별도 스킬로 분리한 날입니다. 동시에 로컬 실행·설치 흐름과 POSCO용 MCP 연결 예시도 보강했습니다.

- 관리자 모드 선호도를 세션 사이에 저장하도록 했습니다. 매번 같은 설정을 다시 켜야 하는 번거로움을 줄이고, 설정 모달과 단축 버튼의 상태가 더 일관되게 보이게 했습니다.
- 관리자 설정 단축 버튼이 활성 상태일 때 파란색으로 보이도록 조정했습니다. 현재 모드가 켜져 있는지 작은 UI에서도 더 빨리 알아볼 수 있습니다.
- OpenWeb 기능을 별도 스킬로 분리했습니다. 웹 열기/조회 계열 작업을 다른 스킬 묶음에서 떼어내어, 필요한 상황에서 더 명확하게 호출할 수 있게 했습니다.
- superpowers 플러그인 전체를 저장소 안에 크게 들고 있던 구조를 줄이고, 필요한 brainstorming 스킬만 로컬 스킬로 정리했습니다. 저장소 잡음을 줄이고 실제 MyHarness 스킬 목록을 더 선명하게 만드는 정리입니다.
- POSCO connectors MCP 설정 예시와 stub을 추가했습니다. 향후 사내 업무 연결 도구를 붙일 때 설정 구조를 확인할 수 있는 출발점입니다.
- Windows 실행 배치 파일과 설치 스크립트를 다듬었습니다. 웹 UI 실행과 로컬 환경 준비가 현재 프로젝트 구조에 더 맞게 이어지도록 했습니다.
- 자동 학습 스킬과 검색 실패 패턴을 정리했습니다. 웹 검색 실패나 YouTube 자막 처리 같은 반복 패턴이 더 재사용 가능한 스킬 설명으로 남도록 보강했습니다.
- project preferences, command help, settings modal, auth, shell utility, system prompt, React backend 테스트를 함께 보강했습니다.

## 2026-05-26 (Myeongcheol)

이날은 한국 법령 MCP와 선택 MCP 호출 흐름을 보강하고, 채팅 공유·아티팩트 표시·파일 도구 결과 표시를 함께 다듬은 날입니다. 사용자가 특정 데이터 소스를 고른 뒤 불필요한 재검색에 빠지지 않고, 결과를 더 쉽게 공유하고 확인할 수 있게 하는 데 초점이 있었습니다.

- 한국 법령 MCP 설정을 추가했습니다. `npx`로 법령 MCP를 실행할 수 있는 기본 구성을 넣어, 법령·판례 질의 흐름을 MyHarness에서 바로 실험할 수 있게 했습니다.
- SQLite 분석 MCP 샘플 데이터를 정리했습니다. 개별 JSON/CSV 샘플을 덜어내고 분석용 SQLite 데이터베이스 중심으로 다루도록 맞췄습니다.
- 채팅 공유용 유틸리티와 UI 흐름을 추가했습니다. 대화 내용을 복사하거나 공유할 때 필요한 텍스트 구성이 더 일관되게 만들어집니다.
- 아티팩트 미리보기와 assistant action 표시를 다듬었습니다. 생성된 파일을 열거나 이어 작업하는 버튼과 카드가 화면에서 더 자연스럽게 보이도록 했습니다.
- 파일 쓰기/편집 도구의 결과 표시를 보강했습니다. 사용자가 보는 출력에서는 작업 맥락에 맞는 경로와 요약이 더 잘 보이게 했습니다.
- `$mcp:`로 특정 MCP 서버를 선택한 경우, 해당 서버의 도구만 노출하는 전용 도구 목록을 사용하도록 정리했습니다. 예를 들어 법령 MCP를 골랐을 때 다른 MCP 도구가 섞여 선택되는 일을 줄여, 사용자가 고른 데이터 소스에 더 집중하게 했습니다.
- 웹 UI에서 메시지를 보내거나 스킬/MCP 목록을 새로고침할 때 MCP 설정을 다시 읽고 새 도구를 즉시 등록하도록 보강했습니다. 설정 파일을 바꾼 뒤 재시작해야만 반영되는 상황을 줄이려는 개선입니다.
- 선택된 MCP 서버 호출에는 반복 제한과 timeout을 넣었습니다. 검색 결과가 없거나 서버 응답이 늦을 때 비슷한 키워드를 계속 재시도하며 대화가 늘어지는 문제를 줄이고, 이미 얻은 결과를 요약하도록 유도합니다.
- MCP의 `[NOT_FOUND]` 결과에서 모델용 경고 문구가 사용자 화면에 과하게 드러나지 않도록 표시를 정리했습니다. 실제 검색 실패 내용과 힌트는 남기되, 내부 지시처럼 보이는 문장은 덜 보이게 했습니다.
- provider 스트림이 오래 아무 이벤트도 내지 않는 경우 최대 대기 시간을 두었습니다. 긴 작업이 조용히 멈춘 것처럼 보이는 상황에서 더 분명하게 실패를 감지하기 위한 안전장치입니다.
- Todo dock의 실시간 활동 줄 수를 세 줄로 줄였습니다. 긴 보고서나 MCP 질의 중 진행 문구가 과하게 쌓여 입력 영역 주변을 밀어내는 일을 줄이고, 최신 작업 흐름만 더 간결하게 보이게 했습니다.
- 한국 법령 MCP 패키지 버전을 고정했습니다. `latest` 변화에 따라 같은 설정이 어느 날 갑자기 달라지는 위험을 줄이고, 테스트와 실제 사용 환경을 더 예측 가능하게 만들었습니다.
- MCP 도구 timeout, 선택 서버 호출 제한, `[NOT_FOUND]` 표시 정리, 선택 MCP 도구 목록, Todo 활동 줄 수 관련 테스트를 보강했습니다. 이번 변경이 실제 질의 흐름과 화면 표시에서 다시 흔들리지 않도록 확인 범위를 맞췄습니다.

## 2026-05-28 (Myeongcheol)

이날은 공공·경제 데이터 MCP를 크게 늘리고, Windows 웹 실행과 자동 학습 스킬 정리를 함께 진행한 날입니다. 여러 외부 데이터 소스를 MyHarness 업무 흐름에서 더 바로 불러올 수 있게 하면서, 로컬 실행과 웹 UI 상태가 덜 흔들리도록 다듬었습니다.

- KOSIS MCP 서버와 기본 설정을 추가했습니다. 통계청 계열 데이터를 MyHarness에서 MCP로 조회할 수 있는 기본 경로가 생겼습니다.
- KOSIS 서버의 SSL 처리와 로컬 실행 호환성을 보강했습니다. Windows 환경에서 웹 UI와 MCP 서버를 같이 띄울 때 인증서·실행 경로 문제로 막히는 상황을 줄였습니다.
- World Bank와 Comtrade MCP 서버를 추가했습니다. 국가별 거시경제 지표와 무역 데이터를 업무 분석에 연결할 수 있는 선택지가 늘었습니다.
- ECOS와 EIA MCP 서버를 추가했습니다. 한국은행 경제통계와 에너지 정보 계열 데이터를 별도 도구로 조회할 수 있게 했습니다.
- 오래된 SQLite 기반 World Bank 샘플 MCP 파일을 정리했습니다. 새 MCP 서버 구조와 맞지 않는 레거시 파일을 덜어내어 설정 혼선을 줄였습니다.
- 웹 방문 기록이 사용자 통계에 더 정확히 반영되도록 고쳤습니다. 웹 UI 사용량·방문 상태를 집계할 때 빠지거나 어긋나는 값을 줄이려는 안정화입니다.
- A4 가로형 보고서 스킬을 PPTX 작업까지 포괄하도록 확장했습니다. 인쇄형 HTML 보고서와 발표자료형 산출물의 역할 구분이 더 분명해졌습니다.
- 자동 학습 스킬을 더 큰 범주로 정리했습니다. 비슷한 실패 패턴이 너무 잘게 쪼개져 쌓이지 않고, command failure와 web research recovery 같은 재사용 가능한 형태로 모이게 했습니다.
- MCP 서버, Windows 런처, React backend, 학습 서비스, 스킬 지침 관련 테스트를 함께 보강했습니다.

## 2026-05-29 (Myeongcheol)

이날은 `skill-mcp` 라우팅을 도움말과 Composer 제안까지 연결하고, 웹 런처 재시작 흐름을 더 단순하게 만든 날입니다. MCP를 일반 스킬처럼 설명하되 실제 실행은 선택한 MCP로 이어지게 하는 구조가 더 선명해졌습니다.

- `skill-mcp` 기반 MCP 라우팅을 명령 도움말과 제안 목록에 연결했습니다. 사용자가 `/help`나 입력창 제안에서 MCP를 볼 때 일반 스킬과 섞여 보이는 대신, MCP 범주와 실행 의도가 더 잘 드러납니다.
- MCP 스킬의 source를 `skill-mcp:<name>` 형식으로 처리하도록 registry와 backend 흐름을 보강했습니다. UI 표시와 실제 라우팅이 같은 기준을 쓰게 하기 위한 변경입니다.
- Composer의 스킬·MCP 제안 표시를 정리했습니다. 사용자가 특정 MCP를 골라 호출하려는 흐름이 더 자연스럽게 이어집니다.
- 웹 런처의 재시작 키를 `R` 중심으로 통일했습니다. 배치 파일과 PowerShell 실행 스크립트가 서로 다른 방식으로 재시작을 안내하던 부분을 줄였습니다.
- 로컬 설정과 인증 manager 쪽 경로 처리를 보강했습니다. 웹 UI 재시작, 포트 설정, 서버 실행이 현재 프로젝트 설정을 더 안정적으로 따르도록 했습니다.
- 명령 registry, Composer, CommandHelpMessage, Windows 웹 런처, React backend 테스트를 함께 늘렸습니다.

## 2026-05-31 (Myeongcheol)

이날은 POSCO 데모용 도움말, 사용량·캐시 표시, Mermaid 차트 조작, 출처 표기 UX, HTML 산출물 출처 각주를 집중적으로 다듬은 날입니다. 답변을 읽는 화면에서는 근거와 비용 정보가 더 분명해지고, 산출물 쪽에서는 출처를 짧게 표시하되 클릭과 hover 근거 확인이 가능해졌습니다.

- POSCO 데모 스킬 트리와 `/help` 표시를 크게 정리했습니다. 여러 POSCO 관련 스킬을 하나의 체계 안에서 더 쉽게 찾고 선택할 수 있게 했습니다.
- 로컬 vector DB RAG MCP 예시를 추가했습니다. 문서 ingest, 저장소, MCP 서버, 안내 문서를 넣어 로컬 Markdown 자료를 검색 기반으로 활용할 수 있는 실험 기반을 만들었습니다.
- cached input token과 캐시 할인 표시가 preset tool schema 흐름에서도 안정적으로 이어지도록 보강했습니다. 사용량 화면에서 cache hit, cached token, 비용 계산이 덜 어긋나게 하려는 변경입니다.
- Mermaid `data-mermaid` 차트에도 확대·축소 컨트롤이 보이도록 했습니다. HTML 산출물이나 메시지 안의 Mermaid 다이어그램을 더 편하게 살펴볼 수 있습니다.
- 대화 기록 검색 도구와 압축·복원 흐름을 보강했습니다. 긴 세션에서 이전 내용을 찾거나 이어받을 때 필요한 내부 경로가 더 안정적으로 연결됩니다.
- 메시지 자동 따라가기 간격을 조정했습니다. 긴 스트리밍 답변을 볼 때 화면이 답변 완료 지점 근처로 과하게 밀리거나 어색하게 멈추는 느낌을 줄였습니다.
- 잘못된 assistant artifact 카드가 화면에 표시되지 않도록 숨겼습니다. 실제로 열 수 없는 산출물 카드가 사용자에게 노출되는 상황을 줄였습니다.
- 본문 안의 출처 링크를 favicon 기반의 작은 출처 칩으로 표시하도록 개선했습니다. 같은 줄 끝에 짧게 붙고, 클릭하면 원문을 새 탭으로 열 수 있습니다.
- 출처 칩 hover 내용을 URL 중심에서 근거 중심으로 바꿨습니다. 첫 줄에는 짧은 주소를, 다음 줄에는 웹 검색·본문 확인 결과에서 직접 가져온 원문 발췌를 따옴표로 표시합니다.
- 출처 정리 중인 스트리밍 상태를 긴 원문 링크 대신 `출처 정리 중...` 칩으로 보여주도록 바꿨습니다. 답변 생성 중에도 본문 줄 흐름이 크게 흔들리지 않게 하기 위한 조정입니다.
- 답변 하단의 전체 출처 목록은 본문 출처 칩과 별도 역할로 유지하면서, favicon과 한 줄 표시를 쓰도록 정리했습니다.
- HTML 보고서·대시보드 같은 시각 산출물의 출처 표기를 짧은 위첨자 각주 형태로 안내했습니다. 예를 들어 본문에는 `(<숫자>)` 전체가 위첨자로 붙고, 클릭하면 원문 링크로 이동합니다.
- HTML 산출물용 고정 출처 CSS marker를 추가했습니다. 모델이 매번 긴 CSS를 출력하지 않고 `<!-- myharness:source-footnotes-css -->`만 넣으면, 파일 작성 도구가 공통 CSS를 채워 넣습니다.
- HTML 출처 각주의 hover tooltip도 주소와 직접 발췌를 자동으로 채우도록 했습니다. LLM이 근거 문구를 다시 요약하지 않고, 이전 웹 검색·본문 확인 도구 결과에서 URL별 근거 조각을 찾아 붙이는 방식입니다.
- `visual-artifact`와 시스템 프롬프트의 출처 지침을 보강했습니다. 보고서 디자인 자유도는 유지하되, 출처 각주 위젯만 일관된 방식으로 쓰도록 정리했습니다.
- 의미 있는 작업 단위가 검증을 통과하면 자동 commit하라는 프로젝트 지침을 문서화했습니다.
- 출처 칩, HTML 출처 각주, Mermaid 표시, 캐시 사용량, POSCO 도움말, 세션 저장·복원, 가격 계산, React 메시지 렌더링 관련 테스트를 함께 보강했습니다.

## 2026-06-01 (Myeongcheol)

이날은 출처 칩과 vector DB RAG 출처 표시를 더 정확하게 다듬고, MyHarness의 기본 작업 방식과 서브에이전트 호출 정책을 정리한 날입니다. 답변 안의 근거 번호가 더 안정적으로 이어지고, 도움말에는 실제 사용자가 직접 호출해야 하는 항목만 남도록 정리했습니다.

- vector DB RAG 결과의 출처 인용 흐름을 고쳤습니다. 로컬 문서 검색 결과가 답변에 붙을 때 근거 조각과 파일 정보가 더 일관되게 전달되도록 했습니다.
- vector DB 출처 칩 표시를 다듬었습니다. 검색 결과에서 가져온 근거가 본문과 출처 목록에서 더 읽기 쉬운 형태로 보이도록 정리했습니다.
- 본문 출처 칩의 표시를 단순화했습니다. 긴 URL이나 과한 장식 대신, 답변 흐름을 방해하지 않는 짧은 칩 형태로 다듬었습니다.
- 출처 번호가 본문과 하단 목록에서 순서대로 유지되도록 고쳤습니다. 여러 출처가 섞인 답변에서도 번호가 건너뛰거나 다시 매겨지는 일을 줄였습니다.
- 출처 목록의 favicon 옆에도 번호가 함께 보이도록 했습니다. 본문에서 본 번호와 하단 출처 목록을 더 쉽게 대응할 수 있습니다.
- 인라인 출처 칩 번호가 문장 안에서 잘못 붙는 문제를 고쳤습니다. 스트리밍 중에도 이미 표시된 번호와 새로 들어오는 번호가 어긋나지 않도록 했습니다.
- `skill-mcp`로 감싼 MCP 서버가 일반 도움말 목록에 중복으로 드러나지 않도록 숨겼습니다. 사용자는 MCP 범주에서 필요한 서버를 고르고, 내부 wrapper는 덜 보이게 됩니다.
- 서브에이전트 직접 호출을 비활성화했습니다. 무분별한 spawned agent 호출보다, 현재 MyHarness 실행 흐름 안에서 명확하게 제어되는 도구와 스킬을 우선하도록 정리했습니다.
- 작업 완료 후 자동 push까지 이어지던 기본 지침을 바꿨습니다. 의미 있는 검증이 끝나도 사용자가 명시적으로 요청하지 않으면 commit/push하지 않고 작업트리에 남기는 쪽으로 운영 기준을 맞췄습니다.
- PGPT 공유 런타임 설정이 있으면 웹 서버가 그 기본값을 우선 쓰도록 했습니다. 저장된 프로젝트 런타임과 화면 시작 상태가 덜 어긋나게 하려는 변경입니다.
- vector DB, 시스템 프롬프트, 명령 registry, 출처 칩, 서브에이전트 비활성화, React backend 관련 테스트를 함께 보강했습니다.

## 2026-06-03 (Myeongcheol)

이날은 긴 작업에서 도구 출력과 세션 문서를 더 잘 회수하고, 도움말을 채팅 메시지가 아니라 화면 위 레이어로 여는 흐름을 정리한 날입니다. 긴 대화나 큰 자료를 다룰 때 필요한 정보는 잃지 않으면서, 화면에는 필요한 만큼만 보이게 하는 데 초점을 맞췄습니다.

- recoverable tool output compaction을 추가했습니다. 긴 도구 출력이 압축되더라도 필요한 경우 다시 찾아 쓸 수 있도록 세션 문서 경로와 연결했습니다.
- 세션 문서 chunk index를 추가했습니다. 긴 문서를 잘게 나눠 저장하고 다시 찾는 흐름이 더 안정적으로 이어지도록 했습니다.
- 인라인 출처 칩 앞의 간격을 복원했습니다. 문장 끝과 출처 칩이 너무 붙어 읽기 어려워지는 문제를 줄였습니다.
- 프롬프트 토큰 칩은 실제로 눌러서 처리할 수 있는 참조에만 렌더링하도록 바꿨습니다. 단순 텍스트가 버튼처럼 보이는 혼란을 줄였습니다.
- `/help`를 채팅 세션에 메시지로 넣는 대신, 화면 위 레이어 팝업으로 보여주도록 바꿨습니다. 도움말을 확인해도 현재 대화 기록이 불필요하게 늘어나지 않습니다.
- 사이드바 도움말도 채팅 세션에 섞이지 않게 했습니다. 도움말은 화면 보조 정보로 보고, 대화 내용은 실제 작업 맥락 중심으로 유지됩니다.
- 도움말 섹션이 펼쳐질 때 미묘한 reveal animation을 넣었습니다. 항목이 갑자기 튀어나오기보다 자연스럽게 열리도록 다듬었습니다.
- PDF 메타데이터가 출처 tooltip에 과하게 노출되지 않도록 필터링했습니다. 사용자는 파일 내부 메타데이터보다 실제 근거 문장을 더 깔끔하게 보게 됩니다.
- 런타임 picker 변경이 조용히 적용되도록 했습니다. 모델·제공자 선택을 바꿀 때 화면과 backend 상태가 필요 이상으로 시끄럽게 흔들리지 않게 했습니다.
- PGPT가 공유 런타임 설정에서 사용 가능 상태로 표시되도록 했습니다. 설정 화면에서 실제 기본 런타임과 표시 상태가 더 잘 맞습니다.
- compact service, session document tool, 도움말 팝업, prompt token, 출처 tooltip, runtime picker 관련 테스트를 함께 보강했습니다.

## 2026-06-04 (Myeongcheol)

이날은 채팅 히스토리 로딩 방식을 바꿔, 오래 쓴 작업공간에서도 사이드바가 더 가볍게 열리도록 한 날입니다.

- 채팅 히스토리를 한 번에 모두 가져오지 않고 점진적으로 불러오도록 했습니다. 대화가 많아진 작업공간에서도 첫 화면이 더 빨리 뜨고, 필요한 만큼 더 이어서 볼 수 있습니다.
- 히스토리 API와 workspace data hook을 함께 조정했습니다. 서버 응답, 프론트엔드 상태, 사이드바 표시가 같은 pagination 기준을 쓰도록 맞췄습니다.
- 사이드바 목록과 스크롤 동작을 보강했습니다. 더 불러오기 흐름에서도 현재 선택한 세션과 목록 상태가 덜 흔들리게 했습니다.
- 서버, history API, Sidebar, workspace data hook, reducer 테스트를 함께 보강했습니다.

## 2026-06-08 (Myeongcheol)

이날은 프로젝트 지침과 도움말 새로고침 흐름을 작게 정리한 날입니다.

- glob 사용 시 `pattern`에는 상대 경로만 넣고, 기준 경로는 `root`로 지정하라는 지침을 AGENTS.md에 추가했습니다. 경로 처리 실수로 파일 검색이 빗나가는 일을 줄이기 위한 운영 기준입니다.
- 도움말 팝업에서 새로고침된 스킬 목록이 바로 보이도록 고쳤습니다. 스킬이나 MCP 설정을 갱신한 뒤에도 오래된 목록이 남아 보이는 문제를 줄였습니다.
- CommandHelpMessage 테스트를 보강해 도움말 갱신 흐름이 다시 깨지지 않도록 했습니다.

## 2026-06-09 (Myeongcheol)

이날은 Gemini 제공자 설정과 채팅 스트리밍 표시를 집중적으로 안정화한 날입니다. 사용자가 실제로 보는 화면에서는 생성 중인 답변이 사라지지 않고, 너무 뭉텅이로 튀어나오지 않으며, HTML 미리보기 준비 상태도 더 분명하게 보이도록 했습니다.

- Gemini compatible provider profile을 추가했다가, 이후 Gemini를 별도 호환 제공자로 분리하지 않고 기존 Gemini endpoint를 편집 가능한 형태로 유지하도록 정리했습니다. 같은 제공자가 중복으로 보이는 혼란을 줄였습니다.
- 저장된 제공자 설정과 기본 제공자 병합 로직을 손봐, 오래된 Gemini-compatible 항목이 다시 UI에 떠오르지 않도록 했습니다.
- live stream 중인 채팅에 다시 들어왔을 때 이미 생성된 내용이 계속 보이도록 고쳤습니다. 다른 세션을 눌렀다가 돌아와도 진행 중 답변이 빈 화면처럼 보이지 않습니다.
- 스트리밍 중인 HTML preview source를 보여주도록 했습니다. 닫는 fence가 오기 전에도 "무언가 만들어지고 있다"는 상태와 받은 소스 일부를 확인할 수 있습니다.
- 채팅 스트리밍 backlog reveal을 부드럽게 조정했습니다. 실제 수신 주기를 바꾸지 않고, 화면에 드러나는 속도만 더 고르게 보이도록 다듬었습니다.
- assistant streaming delta를 batch 처리하고, reveal window 안에 나눠 표시하도록 했습니다. 긴 답변이 한 번에 몰려 나오거나 잠깐 멈춘 것처럼 보이는 느낌을 줄였습니다.
- Gemini 설정, live session rejoin, HTML preview placeholder, StreamingTextRenderer, React backend 테스트를 함께 보강했습니다.

## 2026-06-10 (Myeongcheol)

이날은 스킬 사용 횟수를 추적하고 UI에 표시하는 흐름을 추가한 날입니다. 자주 쓰는 스킬과 최근 작업 맥락을 도움말·입력 제안에서 더 빨리 알아볼 수 있게 했습니다.

- 스킬 실행 상태에 usage count를 저장하도록 했습니다. 사용자가 어떤 스킬을 반복해서 쓰는지 MyHarness가 기억할 수 있는 기반입니다.
- Command Help와 Composer 제안 목록에 스킬 사용 횟수를 표시하도록 했습니다. 자주 쓰는 스킬은 도움말 안에서도 더 눈에 들어옵니다.
- 도움말 텍스트 생성과 backend protocol을 함께 조정했습니다. 서버에서 내려오는 스킬 상태와 프론트엔드 표시가 같은 usage count를 보도록 맞췄습니다.
- skill tool, command registry, backend host, CommandHelpMessage, Composer, reducer 테스트를 함께 보강했습니다.

## 2026-06-14 (Myeongcheol)

이날은 여러 작업 브랜치를 병합하고, 이전에 크게 넣어둔 업무용·법무용 플러그인 묶음과 POSCO 스킬팩 노출을 정리한 날입니다. MyHarness가 업무용 에이전트라는 방향은 유지하되, 저장소 안에 너무 많은 데모 번들이 직접 들어와 목록을 복잡하게 만드는 문제를 줄였습니다.

- session documents, docs/CI maintenance, grep limit 관련 수정, 평가 브랜치 작업을 병합했습니다. 최근 작업 흐름에서 나뉘어 있던 변경을 한 줄기로 합쳤습니다.
- office subagent preset 플러그인 번들을 제거했습니다. 여러 역할 프리셋을 저장소에 직접 들고 있으면서 생기는 목록 복잡도와 유지보수 부담을 줄였습니다.
- legal lite 플러그인 번들을 제거했습니다. 법무용 데모 스킬 묶음은 기본 MyHarness 도움말과 플러그인 구성에서 빠지게 되었습니다.
- POSCO 스킬팩 지원을 Command Help에서 제거했습니다. 데모용 스킬 묶음이 일반 도움말에 길게 노출되어 실제 작업 스킬을 찾기 어려운 문제를 줄였습니다.
- light theme에서 prompt token/chip이 주변 말풍선과 구분되도록 색 대비를 조정했습니다. 실제 렌더링 기준으로 칩 배경과 사용자 말풍선 색이 덜 겹치게 했습니다.
- 스트리밍 Mermaid, WorkflowPanel, session document, prompt token, plugin loader, command registry 관련 테스트를 함께 보강했습니다.

## 2026-06-15 (Myeongcheol)

이날은 한국 법령 MCP 설정을 더 이식 가능하게 정리하고, Mermaid flowchart 확대 보기의 화면 크기 문제를 바로잡은 날입니다.

- 한국 법령 MCP의 cwd 설정을 portable하게 바꿨습니다. 특정 로컬 절대 경로에 덜 묶이게 해, 다른 환경에서도 같은 MCP 설정을 재사용하기 쉽게 했습니다.
- Mermaid flowchart 확대 모달의 크기를 고정 픽셀 상한이 아니라 화면 비율 중심으로 바꿨습니다. 고해상도 모니터에서 확대 보기 창이 지나치게 작게 열리는 문제를 줄였습니다.
- Mermaid 확대 뷰어의 초기 맞춤 여백을 고정값 대신 viewport 비율 기반으로 계산하도록 했습니다. 채팅 안 Mermaid, 아티팩트 iframe Mermaid, 다운로드 HTML Mermaid 브리지에서 같은 기준을 쓰도록 맞췄습니다.
- 확대 뷰어의 흰 배경은 Mermaid renderer가 아니라 MyHarness 확대 캔버스 배경이라는 점을 확인했습니다. 차트 생성 결과와 뷰어 장식 영역을 구분해 다음 UI 조정을 판단할 수 있게 되었습니다.
- Mermaid zoom viewer 크기 회귀 테스트를 추가하고, MarkdownMessage Mermaid, ArtifactPanel, typecheck, UTF-8 guard, Codex 앱 내장 브라우저 화면 점검으로 확인했습니다.
