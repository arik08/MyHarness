---
name: learned-cmd-npm-run-test-react-src-components-tests-composer
description: Use when MyHarness sees this repeated verified failure pattern: cmd input=npm run test:react -- src/components/__tests__/Composer.test.tsx: > test:react
---

# learned-cmd-npm-run-test-react-src-components-tests-composer

This skill was generated automatically from a repeated, verified MyHarness failure pattern.

## When To Use
- Use when MyHarness sees this repeated verified failure pattern: cmd input=npm run test:react -- src/components/__tests__/Composer.test.tsx: > test:react

## Process
1. Read `references/learned-patterns.md` for the concrete observed pattern.
2. Apply the verified corrective path before retrying the failed approach.
3. Keep new evidence concise and avoid storing raw transcripts or secrets.
